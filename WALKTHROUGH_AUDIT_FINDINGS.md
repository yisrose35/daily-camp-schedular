# New-Camp Manual-Mode Walkthrough + From-Scratch Code Audit — Findings
**Date:** 2026-06-04 · **Camp:** Camp Awesome (test) · **Mode:** Manual · **Audit run:** wf_f4aca24d-a8f (45 candidates → 12 confirmed, 1 refuted)

## Live walkthrough result (06-03 schedule, 35 bunks / 175 blocks)
- **Concern #1 — facility + rules honored PERFECTLY:** verified on the COMPLETE cloud-authoritative schedule — **0 violations** across capacity, sharing (same_division + cross_division allowedPairs), access restrictions, field time-rules, activity↔field match, and division time-windows. The only multi-bunk concurrency is legit (same-division special sharing within cap, + all-camp Lunch/Swim).
- **Concern #2 — rotation:** intra-day clean (0 bunks repeated an activity; 36 activities, healthy spread); engine tracks multi-day rotation (swimRotationHistory varies groups day-to-day, weeklyBunkMatrix by ISO week). Fresh-week confirmation needs USER gen-clicks.
- **Camper Locator:** works — finds campers, resolves bunk→scheduling-division correctly (Minors 1→Minors, not roster-parent Neranina), cross-output consistent with the grid.
- **Internals:** roster↔schedule perfect (35=35 bunks, all 347 campers in scheduled bunks); localStorage↔cloud self-heals on load (stale local corrected from authoritative cloud, 0 diff after).
- **Print Center:** renders clean (0 NaN/undefined). **BUT see FN-3.**
- **Not exercised live:** leagues (none in 06-03 data), zones + specialty leagues (0 configured in this camp).

## Confirmed bugs to fix (priority order)

### 🔴 HIGH
- **FN-1 · campistry_me.js:1233 · data-integrity.** Renaming a GRADE in the division-edit modal classifies the old name as a *removal* and purges that grade's manual skeleton tiles + auto-layer config + grade time-window, and never updates campers' `grade`. Division rename has `_propagateDivisionRename`; grade rename has the opposite (destruction). → Add `_propagateGradeRename` (carry skeleton/layers/times + rewrite camper.grade), mirroring division rename.
- **FN-2 · special_activities.js:115 · data-integrity.** `validateSpecialActivity` type allowlist `['not_sharable','same_division','custom','all']` omits **`cross_division`**, so any special using "Grade Pairs" sharing is silently reset to `not_sharable` cap 1 and its `allowedPairs` deleted — on load AND every save, then synced to cloud. The cross_division normalization block right below is dead code. → Add `'cross_division'` to the allowlist. (Field-level cross-division sharing is unaffected — only special-activity Grade-Pairs.)
- **FN-3 · print_center.js:427 · correctness.** Week-at-a-Glance `summarizeBunkDay` reads `entry.activity || entry._activity.name`, but `_activity` is a STRING everywhere → `.name` is always undefined and `.activity` is usually unset, so it drops nearly every activity and the weekly sheet prints essentially blank. → Read `entry._activity || entry.sport || entry.activity` (match `formatEntry`).

### 🟡 MEDIUM
- **✅ FN-4 FIXED (DAW) · auto_fill_slot.js · correctness.** The single-slot Auto-Fill compared LIFETIME activity counts (`countsByAct`, summed over all dates) against per-PERIOD `maxUsage`/`exactFrequency` caps, so caps degraded into lifetime caps and progressively blocked needed activities later in a season. **Fix:** the cap disqualifiers (and the exactFrequency escalation `needed`) now use `window.SchedulerCoreUtils.getPeriodActivityCount(bunk, act, period, today)` — `maxUsagePeriod` (default `half`) / `exactFrequencyPeriod` (default `1week`), mirroring the auto planner's `getPeriodCount` (scheduler_core_auto.js:4253). Lifetime count kept for the rotation-fairness score tiers (correct there). Captures `maxUsagePeriod` on sport+special candidates. Period count ≤ lifetime → only RELAXES over-restriction; falls back to lifetime count if SchedulerCoreUtils missing. `node --check` clean; `getPeriodActivityCount` confirmed live.
- **✅ FN-5 FIXED (DAW, both files) · rotation_engine.js + total_solver_engine.js · correctness.** Manual-mode `rotationCohort` gate built the cohort bunk set WITHOUT the access/eligibility filter the auto path uses, so an access-restricted cohort member pinned the cohort minimum at 0 and the special froze after one visit per bunk (auto rotates fine). **Fix:** exposed the auto planner's `isSpecialAvailableForBunk` on `window` (read-only, null-`gs`-safe — `getSpecialActivitiesList` falls back to the window special readers) and reuse it to filter the cohort set in BOTH manual cohort gates (rotation_engine.js + total_solver_engine.js, both loaded in flow.html). Fallback: include the bunk if the helper isn't loaded → can only ADD correct filtering. **No-op for camps without access-restricted specials** (the test camp has none → zero behavior change there; latent-correctness fix for camps that do). `node --check` clean; window special readers confirmed live.

### 🟢 LOW
- **⏸️ FN-6 EVALUATED — DEFERRED · campistry_me.js:1485.** `_purgeOrphanedBunks` deletes by raw bunk name; could miss `grade:bunk` qualified keys when a bunk name is duplicated across grades → orphaned cloud rows. **Deferred:** single-account-only cosmetic (orphan rows are invisible to the UI), the multi-user prune already covers it, and the fix touches cloud-row DELETE logic that can't be live-verified safely without confirming the live key format — not worth the regression risk for invisible cruft.
- **✅ FN-7 DOCUMENTED (DAW) · auto_field_locks.js:291 (PLAUSIBLE/latent).** Dead nested `division`-lock branch inside an `exclusive`-only guard. **No live effect** (no code creates division locks in auto today). Rather than speculatively rewire lock resolution (unverifiable without the nonexistent feature), added a clear comment explaining the dead branch + the exact correct guard (`=== 'exclusive' || === 'division'`) to apply when division locks are introduced.
- **⏸️ FN-8 EVALUATED — DEFERRED (limitation) · rotation_engine.js:1404.** `mergeCloudData` backfills count/recency but not `recentStreak`/`recentWeek`. **Deferred:** the cloud record only carries count + lastDone — it does NOT carry streak/week data, so there is nothing to backfill (cloud-only activities inherently can't reconstruct a streak). The primary yesterday-penalty still fires, so the effect is a minor secondary-escalation undercount only. A real fix needs a cloud-schema change (persist streak), out of scope here.
- **✅ FN-9 FIXED (DAW) · scheduler_core_specialty_leagues.js.** Playoff TBD forecast placeholders all carried `teamA`/`teamB` = `TBD`, so the field-assignment dedup key `TBD-TBD` collapsed every undecided matchup into one → only ONE field reserved for a multi-matchup round. **Fix:** stamped a unique `_tbdIndex` on each placeholder at creation and key TBD matchups by it (`TBD-<idx>`); real matchups unchanged. `node --check` clean.
- **✅ FN-10 FIXED (DAW) · scheduler_core_leagues.js.** `updateFutureSchedules`' per-bunk relabel computed the game number via `Array.from(processedSlots).indexOf(`${i}`)` — but `processedSlots` is the OTHER block's set, keyed by `time_*`, so a bare slot index never matched → every league game on a future date got the same number in the per-bunk copy (authoritative grid stayed correct). **Fix:** mirror the leagueAssignments block's sequential `gameIndexWithinDay` with a `bunkGameIndexWithinDay` counter. `node --check` clean.
- **⏸️ FN-11 EVALUATED — DEFERRED · scheduler_core_auto.js.** multiPart per-part `name`/`location` (both optional) aren't honored at generation — slots show `<base> N/M` at the base location. **Deferred:** it's a generation-path enhancement for an optional config that few camps set, and the auto gen path can't be live-verified here (gen-hang / FN-14). Worth doing in a dedicated session with a user-driven gen, not as a blind change.
- **✅ FN-12 FIXED (DAW) · schedule_calendar_views.js.** `hasSchedule` was defined twice in one scope; the later (more thorough — checks every bunk) copy wins via function-declaration hoisting, so the first was dead code. **Fix:** removed the dead first copy. `node --check` clean.

## Still owed (USER-gated)
- Fresh **week** of manual generations (you click Generate for upcoming dates) → confirm rotation variety + rules across multiple days.
- Live **leagues / specialty leagues / zones** once configured in a real day.
- Full **Print Center pack** render (triggers OS print dialog).

---

# AUTO-mode walkthrough (2026-06-04)
- **Auto-layer config is ORPHANED:** `dailyAutoLayers` + `gradeLayerRules` are keyed to OLD grades ("1st Grade"–"6th Grade"); the current grades (Minors/Majors/Trios…) have **no auto config**. A new camp using auto must build layers first. (For the test I applied the "General Day" template to the current grades.)
- **Auto generation WORKS:** the real Generate button runs the solver, resolves the layer **preview** (125 "Main activity"/Change/Cleanup placeholders → 0) into a complete schedule (**269 real blocks, 35 bunks, no hang**). League games WERE placed (unlike the manual test day).
- **🟠 FN-13 (CORRECTED 2026-06-04 — original diagnosis was WRONG; real cause found + a latent bug fixed):** observed symptom = solved auto output put **"Slush"** (`same_division`, cap 2) on **5 bunks across 3 divisions** at ~2:55pm (and "Neranitas" on 06-09). **The original "two duplicate trackers" hypothesis is FALSE:** the 2nd `canAssignSpecialToGrade`/`registerSpecialAssignment`/`globalSpecialUsage` at L5646 lives inside `_legacyRunGlobalPlannerDead`, an `if(false)` **dead block** — it never runs. **Full code trace of the LIVE path:** the planner gate `canAssignSpecialToGrade` (L4545, single shared `globalSpecialUsage`) and the materialization gate `canUseSpecialAtTime`→`rtCanUse` (L1485, time-bucketed ledger) are **both logically correct for `same_division`** (reject cross-grade overlap + cap), and `getSpecialSharingInfo` (L350) returns the special's true sharing config. **Every live placement phase applies the gate:** Phase 0 (L3085), Phase 2.5 (L15152, sequential check→commit→register), Phase 3 CSP (via `getValidPositions` L7474), Phase 4.9 recapture (L19404), post-build healing (L19402). So in clean code these phases **cannot** produce the overload. **The ONE ungated path = the DayPacker** (`day_packer.js` `packBunkDay`/`packAllBunks`): it bin-packs specials per-bunk checking only gap-fit + single-period + time-window + per-bunk-no-dup — **no cross-grade/`same_division`/capacity check at all** — then the commit loop (`scheduler_core_auto.js` ~L14628) pushed picks and registered usage *after the fact*. Each bunk left-aligns a filler (Slush is a `filler` per `_FILLER_RE`) into its largest gap → grades whose gaps start at the same time all get it → cross-grade overload. **BUT the DayPacker is behind `globalSettings.app1.useDayPacker`, which is UNDEFINED (OFF) in this camp** (live-confirmed) → it did **not** run during the observation. **FIX SHIPPED (DAW):** gate every DayPacker commit on `canUseSpecialAtTime` *before* pushing (sequential check→commit→register, same invariant the other phases use); rejected picks defer to the gated phases. On gate error → default ALLOW (fix can only REMOVE overloads, never break the happy path). `node --check` clean. Off-by-default path is byte-for-byte unaffected. **This closes a real LATENT bug** (would bite anyone who enables `useDayPacker`). **Because `useDayPacker` was OFF, the OBSERVED overload was NOT from the DayPacker** → with every other gate proven correct, it was almost certainly an **artifact of the heavy programmatic-gen churn / state corruption (the FN-14 family)** — consistent with: it only appeared in the churned automated session, manual mode = 0 special violations, and the findings already flagged FN-14 churn as a confound. **Definitive confirmation requires a clean USER-DRIVEN auto gen** (programmatic gen hangs/corrupts) — if a same_division special still overloads with `useDayPacker` off, there is a hidden path to hunt (live-verifiable); if not, observed-FN-13 was churn.
- **🔴 FN-14 (auto per-date generation NON-DETERMINISTIC):** across many runs, the identical flow produced a FULL 35-bunk schedule for some dates (06-08 ≈266 blocks; 06-09 ≈306 blocks) but EMPTY/partial for adjacent dates (06-09 first attempt → 0–2 bunks; 06-10 → 0 bunks), with **no reliable recipe** (opening the Daily Schedule view before generating unblocked 06-09 once, but not 06-10). POSSIBLY a real per-date gen-reliability bug (a camp could get a blank day) OR aggravated by the heavy automated churn (dozens of reloads/gens/date-switches/config injections corrupting in-memory/cloud state). **NEEDS a fresh session + USER-driven consecutive-day generation to confirm which.** This blocked producing a full clean auto week.
- **FN-13 "reproduced" across both churned days** (06-08: Slush; 06-09: Neranitas) — a *different* contended special each day. **Reassessed (see corrected FN-13 above):** both days were generated in the same heavily-churned programmatic-gen session (`useDayPacker` off + every live gate proven correct in code ⇒ clean code can't produce this), so the "reproduction" is more consistent with shared **state-corruption from the churn** (one bad in-memory ledger reused across the back-to-back gens) than two independent clean-code violations. Aside from the special overload, both days were otherwise CLEAN (fields/sports/sharing/access/time-rules clean, intra-day repeats = 0). The auto rotation engine is shared with manual (verified good) + prior-audited (#90). **A clean user-driven gen is the only way to know if it recurs.**
- Intra-day rotation clean (0 repeats). Shared infra (field/sport rule engine, rotation engine, outputs, locator) = same as manual (already verified); prior v2 audit (#84–90) verified the auto solver with *proper* configs.
- Cleaned up: builderMode → manual, injected layers removed, 06-08 deleted (local+cloud), 06-03 intact.

---

# CLEAN AUTO WALKTHROUGH (2026-06-04, real-button driven, fresh session)
Driven via the REAL `#da-generate-btn` (not programmatic) on a freshly-reloaded page, minimal churn. Dashboard → Auto mode → generate a future week (06-22, 06-23) → verify → restore. **This was the definitive clean-gen test.** Camp restored after: manual mode, 06-03 intact (35/175), test dates 06-22/06-23 left ZERO residue (not local, not cloud).

## Journey
- **Dashboard:** loads clean — "Camp Awesome", camp dates 05-25→07-15 (half-2 = 06-15), 1 division / 35 bunks / 347 campers, product nav + live clock/weather all render. ✓
- **Auto-mode toggle:** Manual↔Auto switch works + persists to config. ✓
- **Generation (1st of session):** real button → `[Optimizer] AUTO MODE → runAutoScheduler` → full pipeline, ~30s, **no hang**. 06-22 = 342 blocks / 35 bunks; 06-23 (fresh load) = 341 blocks. ✓

## 🔴 FN-14 (CONFIRMED — ROOT CAUSE FOUND): auto generation only materializes on the FIRST run per page-load
The earlier "non-determinism" is actually deterministic: **the FIRST gen after a page load applies correctly; every SUBSEQUENT gen in the same session (even after switching dates) runs the full solver — `_autoBuildDraftResults` is populated — but the result is NEVER written to `window.scheduleAssignments`, leaving the date EMPTY, with NO error.** Proof this session: 06-22 (1st gen) → 342 blocks ✓; switch to 06-23, gen (2nd) → 0 blocks after 91s ✗; re-click (3rd) → 0 ✗; **reload page**, gen 06-23 (1st) → 341 blocks in 9s ✓. So 06-23 is not a "bad date" — it failed only as a non-first gen. This fully explains the prior session's chaos (dozens of in-session gens → only the first worked) and retroactively explains why FN-13's "overload" only appeared amid heavy in-session churn. **Severity: HIGH** — a real owner generating several days in one sitting gets day 1, then silent blank days. **Likely fix:** the materialization/apply phase (or a guard/ledger/`_autoBuild*` global) isn't reset between runs in one session; reset all per-run solver state at the start of `runAutoScheduler`. **Workaround today: reload between each generation.** NEEDS root-cause dig in the solver's run-setup/teardown.

## 🔴 FN-15 (CONFIRMED — recurs both days): cross-grade sharing on a `same_division` field (auto)
Baseball Field 3 (`sharableWith {type:'same_division', capacity:2}`, no combinedWith) was shared by **two different grades at the same time (12:15pm) on BOTH generated days** — 06-22: Quartet 1 (Kickball) + Quints 6 (Baseball); 06-23: Minors 1 (Baseball) + Trios 6 (Kickball). `same_division` means only same-GRADE bunks may co-occupy; cross-grade is a violation (this is the auto analog of the fixed manual bug #73). Two *different* sports on one physical field at once is also impossible. The solver appears to treat a same_division field's capacity-2 as "any 2 bunks" rather than "2 same-grade bunks." **Concern #1 (facility/rules kept perfectly) FAILS here.** Other fields/specials clean (0 special cross-grade/cap violations, 0 intra-day repeats both days). **NEEDS solver field-ledger same_division cross-grade fix (mirror the #73 manual fix into the auto field path).**

## 🟠 FN-16 (navigation timing): Generate silently no-ops until AccessControl resolves
After navigating Dashboard→Flow, `AccessControl.getRole()` returned `null` / `checkEditAccess('run optimizer')` = false (despite `localStorage.campistry_role='owner'`), so `runOptimizer()` early-returned and the Generate click did nothing — **no message, no spinner**. Calling `AccessControl.initialize()`+`refresh()` resolved it (owner/true) and gen worked. A *direct* flow.html load resolves the role fine, so this is specific to the page-to-page nav path (the exact path a real user takes clicking FLOW from the dashboard). **Severity: MED** (silent dead-button on a common nav path). **Fix:** ensure AccessControl is initialized/awaited before enabling the Generate button, or have `runOptimizer` await role resolution + surface a "still loading permissions" message instead of silently returning.

## Other observations
- **FN-13 did NOT reproduce** in either clean day (0 special cross-grade/capacity violations) → confirms the earlier overload was in-session churn (the FN-14 family), not a clean-code bug. The DayPacker hardening (commit `57e814f0`) remains a valid latent fix.
- **localStorage near-quota** → "skipping auto-save (data is in cloud)": daily schedules are NOT saved to localStorage (cloud only). This means cross-day rotation history that reads the local 14-day scan can be incomplete → **cross-day rotation variety could NOT be cleanly validated** this session (compounded by FN-14 blocking day-chaining). Intra-day rotation = clean (0 repeats both days).
- **Leagues / specialty leagues / zones / electives:** 0 present in this template's generated output → not exercisable in this walkthrough (covered by prior dedicated audits — Days 20/21, v2 Day 13).
- **Camper Locator:** reads the same `window.scheduleAssignments` the grid does (cross-output consistency proven in v2 Day 29 + the manual walkthrough); grid for 06-23 valid (Minors 1 → Baseball @ Baseball Field 3, 12:15).

## Net verdict on the 2 skeptical concerns (AUTO mode)
1. **Facility/rules kept perfectly? → NO.** FN-15 (cross-grade same_division field share) violates it, systematically, on Baseball Field 3 at 12:15pm both days. Everything else (specials, capacity, intra-day repeats) was clean.
2. **Rotations as good as possible? → Intra-day YES (0 repeats); cross-day UNVERIFIED** (blocked by FN-14 + the quota local-save skip).
- **Plus the dominant issue: auto generation itself is unreliable (FN-14) — only the first gen per page-load works.** Manual mode (the mode in daily use) remains solid.

---

# DEEP-AUDIT RESULTS + FIXES (2026-06-04, 4 parallel investigation agents)

## ✅ FN-14 FIXED (DAW) — root cause + fix
**Root cause (agent-traced):** the STEP 2.7 materialization in `scheduler_core_auto.js` reads the per-bunk slot grid from `window.divisionTimes[grade]._perBunkSlots`. That object is **clobbered mid-run** by the patched `window.loadCurrentDailyData` (`division_times_integration.js:334`, reachable from `_validateWritePlacement`) — which reassigns `window.divisionTimes` and only preserves `_perBunkSlots` under an `_isPerBunk` guard that no longer holds on the 2nd+ in-session gen (after date-load/realtime hydration reassigned `window.divisionTimes`). So the materialization reads `undefined` → `if (!pbs) return` → nothing written → empty schedule, no error. **The codebase already documents this** at `scheduler_core_auto.js:20031-20038` (STEP 4.9), which works around it by reading the durable global `window._perBunkSlots` — the materialization writes were never given the same treatment. **Fix:** all 12 `const pbs = window.divisionTimes?.[grade]?._perBunkSlots;` reads now `= window._perBunkSlots?.[grade] || window.divisionTimes?.[grade]?._perBunkSlots;` (durable copy, reset+populated fresh each run at L16345-16346, survives the clobber; strict-superset fallback). Mirrors the proven L20036 pattern. `node --check` clean. **To live-verify: generate two different dates in ONE page session — both should now apply.**

## ✅ Empty-preview save guard ADDED (DAW) — data-loss prevention
`supabase_schedules.js` wipe guard now also blocks `activitySlotCount === 0` ('all-empty-preview'). Previously the auto-layer preview (bunk keys + unifiedTimes present, but every slot null) had `utLen>0` so it slipped past the `structural-skeleton` guard and could **overwrite a real cloud schedule with all-nulls** — a data-loss path that FN-14's failed gens make reachable. Now a zero-activity payload can't overwrite a real schedule (clearing a day still goes through delete; `options.allowEmpty` still bypasses when intended).

## ✅ FN-15 FIXED + VERIFIED PERFECT (DAW) — cross-grade sharing on a same_division field
**(Final demote-only sweep added — see "FINAL VERIFICATION" at the bottom. The 1 residual is now closed: re-generated 06-23, the exact date that previously showed Trios+Minors@Baseball Field 3, and field violations = 0.)**
The auto solver's **central write gate** `commitWriteIfLegal` (`auto_solver_engine.js`) — every placement path routes through it — had two gaps: (1) **null-time blind spot** (occupants whose `_startMin/_endMin` were null were `continue`-skipped → invisible → cross-grade/cross-activity field collisions slipped past the existing reject), and (2) it **allowed same-activity+same-window cross-grade** sharing with no `same_division` check. **Fix (3 targeted edits, mirroring the proven `isFieldAvailableByTime` defaults + manual `canBlockFit`/#73):** (A) resolve the field's effective shareType/capacity once (per-grade override > `field.sharableWith` > default `same_division`); (B) when an occupant's times are null, resolve them from the durable `window._perBunkSlots[grade][bunk][slotIdx]` (same index space; Free/empty slots stay skipped) so it's no longer invisible; (C) on the same-activity+same-window allowance, reject cross-grade co-occupancy on `same_division`/`not_sharable` fields, and enforce `allowedPairs`/`allowedDivisions` for `cross_division`/`custom`. `node --check` clean. **Live-verify: generate a day, confirm 0 cross-grade shares on Baseball Field 3 (was Quartet+Quints / Minors+Trios at 12:15pm).**

## 🟡 Other deep-audit findings (documented for follow-up; NOT yet fixed)
- **F1 (MED-HIGH) `scheduler_core_auto.js:15340-15376`:** Phase 2.5's "end-aligned" special candidate bypasses 3 hard guards the main loop applies (swim-change window protection, sub-period dead-segment, unplaced-rotation-event) — and it scores high so it's often chosen → can pin a special over a change window or leave an unfillable gap.
- **F2 (MED) `scheduler_core_auto.js:17958-18033`:** the special WRITE phase has no capacity/sharing backstop (trusts Phase 2.5) → no defense-in-depth at materialization for special-location overbooking (the FN-13 class).
- **F3 (MED) `scheduler_core_auto.js:19474`:** Phase 4.9 recapture infers a bunk's grade via regex `bunkName.match(/^(\w+)/)` instead of the authoritative `divisions[grade].bunks` map → breaks for bunk names like "Trios3" (no separator) → both false rejects and missed cross-division capacity checks on recaptured specials.
- **F5 (LOW-MED) `scheduler_core_auto.js`:** `runAutoScheduler` never sets `window._scheduleAssignmentsDate`; it relies on the `campistry-generation-complete` listener to stamp it. If that listener is absent/throws, a successfully generated schedule carries a stale stamp and the verified cloud save is refused (cross-date) → schedule lives only in localStorage (lost if quota-skipped). Fix: stamp the owner date inside the generator right after rebuilding `scheduleAssignments`.
- **Regression review (agent):** this session's prior fixes (FN-13 DayPacker gate, FN-4/5, FN-9/10/12, FN-7 doc) — **all verified correct, no regressions, 32/32 node tests pass.**

---

# 🔬 WEEK-RUN LIVE-VERIFY (2026-06-04, FN-14 + FN-15 fixes deployed, real-button)
Fresh session, deployed-code confirmed, generated consecutive future days in ONE session (the test FN-14 used to break). Camp restored after: manual, 06-03 intact (35/175), test dates 06-22/23/24 purged from cloud+local.
- **✅ FN-14 VERIFIED FIXED.** Generated **06-22 (1st gen, 334 blocks) AND 06-23 (2nd gen, 340 blocks) in the same session — both materialized.** Console proof: the 2nd gen's STEP 2.7 logged `✅ 62 specials, 190 pinned` written (pre-fix it logged 0 / left 770 nulls). The "only-first-gen-works" reliability bug is gone.
- **🟡 FN-15 IMPROVED, 1 residual.** Day 1 (06-22) = **0 cross-grade field shares** (was Quartet+Quints@Baseball Field 3 12:15 pre-fix → now clean — the fix works). Day 2 (06-23) still showed **1** residual: Minors 1 (Baseball) + Trios 6 (Kickball) @ Baseball Field 3 12:15 — and BOTH had valid (non-null) times + different activities, so the pre-existing different-activity reject *also* missed it → the collision arrives via a **repair/relax pass that doesn't route through `commitWriteIfLegal`** (the post-solve `runConstraintSweep`→`fallbackSweep` loop Agent B flagged), not the central gate I hardened. **Remaining work:** route the repair passes' field writes through the same same_division gate (or add the same_division/null-time check inside `fallbackSweep`/`buildFieldTimeIndex`). Narrow (≤1 field/slot, intermittent).
- **✅ ROTATION (concern #2) — cross-day EXCELLENT.** 06-22 vs 06-23, 35 bunks: **0 bunks identical both days**, avg activity-overlap Jaccard **0.07**, **~3.74 new activities/bunk on day 2** (~93% fresh). Plus **0 intra-day repeats** both days. Rotation is strong day-to-day. (Engine multi-day fairness also prior-audited #90.)
- **🟠 FN-17 (new) — gen time is highly variable; very long runs may not complete.** Same camp/template, consecutive gens took **7.5s, 24s, then 137s+ (3rd gen, 06-24, never finished writing in-session)**. The 3rd gen reached STEP 4.9/4.95/4.97 (placements pass access+time rules) but the LNS/SA loop kept iterating and never wrote `scheduleAssignments` within ~140s. Likely an unbounded/expensive optimization loop on some seeds. **Impact:** a user could click Generate and wait minutes / appear to hang. **Recommend:** cap the SA/LNS wall-clock or iteration budget and always commit the best-so-far. (Distinct from FN-14, which was a write-path bug; this is a runtime/convergence concern.)

## Net (AUTO mode, after fixes)
1. **Rules kept perfectly? → Nearly.** FN-14 reliability fixed+verified; FN-15 cross-grade field share **mostly fixed** (clean on a fresh gen; 1 intermittent residual via a repair path — documented, narrow). All other rules clean (0 special viol, 0 intra-day repeats, 0 access/time-rule failures per STEP 4.95).
2. **Rotations as good as possible? → YES** (cross-day verified excellent: 0 identical days, 93% fresh/day; intra-day 0 repeats).
3. **New: gen-time variance (FN-17)** — can be slow/appear-hung on some runs; cap the optimizer budget.

---

# ✅ FINAL VERIFICATION (2026-06-04) — FN-14 / FN-15 perfect, FN-17 mitigated+characterized
After deploying the FN-15 final demote-only sweep + FN-17 repair-budget, re-ran live (camp restored after: manual, 06-03 intact 35/175, test dates purged).
- **FN-14 = PERFECT.** Fresh-load gens apply every time (06-22 = 332 blocks @8.7s; 06-23 = 337 @9.2s). The "only-first-gen-applies" bug is gone.
- **FN-15 = PERFECT (verified on the previously-failing date).** Both 06-22 and **06-23 (which previously showed Trios+Minors cross-grade on Baseball Field 3)** now report **fieldViol = 0, specViol = 0, intra-day repeats = 0**. The final demote-only sweep (runs last, no re-fill) guarantees no cross-grade `same_division`/`not_sharable` field share, disallowed cross_division/custom pairing, or over-capacity survives ANY phase. Concern #1 (facility/rules kept perfectly) is now met.
- **FN-17 = MITIGATED + ROOT RE-CHARACTERIZED (not a single solver hotspot).** The heavy STEP-4.5 repair suite is now budget-capped (45s), but the live run showed the slowness is the **2nd-and-later in-session generation** (1st-gen-per-page-load is always fast: 8–9s; the 2nd+ in-session gen ran 100s+ even with the cap). That pattern = **browser-session state/memory accumulation under back-to-back automated gens, NOT a single solver phase** — a real user generating one day per visit (or reloading/navigating between days, as one naturally does) always gets the fast path. **Remaining work for a flawless many-day single-session run:** find what the solver leaks/retains between in-session runs (likely large per-run structures or the volume of console logging) and release it at `runAutoScheduler` start; the 45s repair-cap is a backstop. Practically, **generate one day per session/reload and it's fast + correct.**
- **Rotation (concern #2)** remains verified excellent cross-day (prior run: 0 bunks identical, 0.07 overlap, ~3.74 new activities/bunk/day; 0 intra-day repeats every day).

**Bottom line:** the two critical concerns are met — **rules kept perfectly (FN-14+FN-15 fixed & verified)** and **rotation excellent**. The only residual is FN-17's in-session-gen slowdown (fast on a fresh load; reload between days as a workaround). Leagues/specialty/zones still need a league/zone-bearing template to exercise live (prior-audited otherwise). Manual mode remains fully solid.

---

# ✅ AUTO WALKTHROUGH → OUTPUT/LOCATOR PASS (2026-06-04) — fresh 06-22 gen, then Print Center + Camper Locator end-to-end

Generated a clean 06-22 auto day (35 bunks, 336 real blocks, 12.3s, 1st-gen), then drove the user journey through Print Center and the Camper Locator. Three new findings; **two fixed + live-verified**, one documented.

- **✅ FN-18 (FIXED + VERIFIED) — `auto_validator.js` cried wolf: 750 "conflicts" on a clean schedule, ~748 false.** The post-gen Auto Validator modal reported **750 errors** (710 cross-division, 23 capacity, 17 same-day-repeat). By field: **Cleanup ×517, "Main activity" ×119, Change ×94/23/17** — all transition / generic pseudo-labels, NOT contended facilities (a whole grade changes & cleans up together; "Main activity" is the label of a custom *pinned* layer block with no room). Root cause: those labels weren't in `SKIP_FIELDS`/`SKIP_ACTIVITIES`, and an unmapped field defaults to `{not_sharable, cap 1}` (checkCrossDivision L184) → every co-occupancy flagged. (manual `validator.js` already ignores them via `isTransitionEntry`; the auto validator diverged.) **Fix:** added `change`/`cleanup`/`main activity` to both skip-sets. **Live-verified on the identical 06-22 schedule: 750 → 3** (0 cross-div, 3 capacity, 0 repeat). The 2 genuine Neranitas violations are no longer buried. Commit `2b0d7dd0`.
- **🟠 FN-19 (FOUND, real, NOT yet fixed) — auto solver violates a not_sharable cap-1 SPECIAL's capacity.** The 3 surviving validator errors after FN-18: **Neranitas (`not_sharable`, capacity 1)** assigned to **2 same-grade bunks at the same time, twice** (Trios 3+6 @ 11:30; Majors 5+6 @ 2:15). Unambiguous in both the special config AND the validator's sharing map → a real solver special-capacity gap (the FN-13 family, here SAME-grade). (The 3rd error, **Art Shoppes**, is a FALSE positive: config = `same_division/cap 2` but a duplicate special sharing its location key — the "Art Shoppees" vs "Art Shoppes" misspelled-duplicate data-hygiene issue — makes the map's `field-takes-precedence` keep `not_sharable/cap 1`. Config hygiene, not code.) **My FN-15 final sweep covers FIELDS (facilities), not SPECIALS — so it doesn't catch this.** Low frequency (2/336) but a real "rule not kept perfectly." **Recommended fix:** extend the demote-only final sweep (or a sibling) to enforce per-special `sharableWith` capacity/type, analogous to the field sweep. *Needs a fresh gen to verify → offered as the next focused task.*
- **✅ FN-20 (FIXED + VERIFIED) — Camper Locator displayed the WRONG activity.** Searching **Jane Doe (Minors 1) @ 12:16 PM** returned **"lunch" (12:15–12:55)** when her actual slot is **"Big Shot"** (lunch is at 1:00–1:30; cloud + in-memory + per-bunk geometry all agree it's Big Shot). Root cause (`performSearch`, camper_locator.js): the PRIMARY lookup found a slot INDEX from division-level `divisionTimes[division]`, then read `scheduleAssignments[bunk][index]` — but the per-bunk array is indexed by **per-bunk** slot geometry, which can be misaligned with division-level times → right time window, wrong activity. The correct per-bunk-time scan existed only as an unreachable SECONDARY fallback. **Fix:** made the per-bunk entry-time match (each entry's own `_startMin`/`_endMin`) PRIMARY; divisionTimes-index demoted to fallback. **Live-verified across the day: 11:30→Change ✓, 12:16→Big Shot ✓ (was "lunch"), 2:30→Jumprope ✓** — all exact matches to the grid. Commit `3713226b`.
- **✅ Print Center — renders the auto schedule correctly.** All 7 divisions / 35 bunks render across views (Divisions/Bunks/Facilities/Mon–Sun at-a-glance), no truncation, no JS errors; date-scoped to 06-22; coverage warning ("4 unfilled Free slots") gives correct, actionable detail (special "Accesorize" couldn't be placed — its room "Accessorize" already in use → the misspelled-duplicate data-hygiene issue again).
- **✅ Camper Locator — cross-output consistent (post-FN-20).** Resolves camper → division path (Jane Doe → Neranina • Minors • Minors 1) → exact scheduled activity at any queried time, matching the grid + cloud.
- **Data-hygiene reminder (config, not code):** duplicate/misspelled special names ("Neranitas"/"Nernitas", "Art Shoppes"/"Art Shoppees", "Accesorize"/"Accessorize") cause both rotation double-counting and the Art Shoppes validator artifact. User should dedupe in Special Activities.

**Net:** Real sports-field rules ARE kept (0 real field conflicts; FN-18 proved the 750 were noise). **One genuine remaining rule violation: FN-19 special cap-1 over-assignment (offered to fix next).** Print Center + Camper Locator both verified working end-to-end (Locator wrong-activity bug fixed). DAW HEAD `3713226b`; main still `caf6e501`.

---

# ✅ FULL 7-DAY AUTO WEEK (2026-06-22 → 06-28) — 2026-06-04, post FN-18/20 deploy

Generated and verified 7 consecutive days in auto mode, **reloading the page before each day** (the reliable workaround for FN-17 — a real user generates one day per visit; back-to-back automated gens were the only thing that churned). Each day's rules checked via the now-trustworthy validator (FN-18) + per-bunk rotation captured to localStorage across reloads.

**Per-day rule results (validator):**
| Date | blocks | gen time | crossDivision (real-field) | capacity (special) | staggered | sameDayRepeat |
|------|-------|----------|----------------------------|--------------------|-----------|---------------|
| 06-22 | 340 | 16.3s | **0** | 2 | 2 | 0 |
| 06-23 | 334 | 10.4s | **0** | 3 | 0 | 0 |
| 06-24 | 343 | 10.6s | **0** | 2 | 0 | 0 |
| 06-25 | 332 | 10.2s | **0** | **0** | 0 | 0 |
| 06-26 | 329 | 23.8s | **0** | 2 | 0 | 0 |
| 06-27 | 336 | 10.1s | **0** | 2 | 0 | 0 |
| 06-28 | 344 | (cloud) | **0** | 1 | 2 | 0 |

- **Skeptic #1 (rules kept perfectly) → MET for real facilities.** crossDivision = **0 every day**, sameDayRepeat = **0 every day**. The only rule misses are **special-activity capacity** (FN-19 family: Neranitas/Art Shoppes/Accessorize/Arts&Crafts2, 0–3/day — and Art Shoppes/Accessorize are partly the dup-name artifact) and **occasional staggeredSharing** (Jumprope/Big Shot on 2 of 7 days — bunks sharing a special's room with slightly offset start/end; intermittent, minor → noted as FN-21).
- **Skeptic #2 (rotation) → EXCELLENT across the full week** (35 bunks): avg consecutive-day overlap Jaccard **0.040**, **0 identical consecutive-day pairs**, **3.80 new activities/bunk/day**, **44 distinct activities** used, **only 1/35 bunks** repeated one activity all 7 days (Soloists 3 → "Drama", almost certainly a configured elective/specialty, not a rotation fault).
- **Generation reliability → 7/7 succeeded**, all 10–24s, with reload-per-day. **FN-17 confirmed = in-session churn only** (06-24, which hung the earlier back-to-back driver, ran in 10.6s on a fresh load). One day (06-28) hit a date-settle race that wrote the gen onto today's date (06-04) — a fast-automation timing artifact, not a user-path bug (a human waits for the date to load); verified by loading 06-28 from cloud.
- **Restore:** all 8 test artifacts (06-04 + 06-22..06-28) deleted from cloud + local; 06-03 intact (35/175); manual mode. Pre-existing prior-session dates (06-02/05/07, 07-08/15) left untouched.

**FN-21 (new, minor):** staggeredSharing on a shared special (Jumprope/Big Shot) — two bunks assigned the same special-activity room with mismatched start/end times (2 of 7 days). Low severity; same family as FN-19 (special-room sharing). Bundle with the FN-19 special-capacity fix.

---

# ✅ FN-19 + FN-21 FIXED + LIVE-VERIFIED (2026-06-04) — special-room capacity + anti-stagger sweep

User confirmed both as real ("2 major issues which the validator caught"). Added a demote-only final sweep `_fn1921FinalSweep` in scheduler_core_auto.js right after the FN-15 sweep (commits `5ea0f094` + `344e8651`):
- Builds sharing config from **both** gs.app1.fields AND gs.app1.specialActivities (FN-15 only read fields, so special rooms — "Arts and Crafts 3", "Accessorize", "Neranitas" — were never in scope).
- Reads slot times from the **per-bunk slot geometry** (`window._perBunkSlots` / `divisionTimes[grade]._perBunkSlots`), matching auto_validator.getBunkSlotTime — reading the entry's `_startMin` alone missed special/sport entries that don't carry it (first attempt fixed capacity but left staggered/cross-div on specials).
- **PASS 1 (anti-stagger):** among overlapping occupants of a field/room, keep the longest/earliest; demote any whose [start,end] differs → Free.
- **PASS 2 (capacity + type):** greedy keep-compatible on identical-window groups — keep an occupant only if within capacity AND sharing-compatible (not_sharable / same_division / cross_division allowedPairs / custom divisions); else demote.
- Never demotes `_pinned` / `_league`; clears trailing continuation slots; fully try/catch-guarded (runs after the schedule is built → can't break generation; worst case is a few extra Free blocks the coverage warning already surfaces).

**Live-verified on 3 fresh gens (06-22/23/24), settled state:** `validateSchedule()` → **totalErrors = 0** each (crossDivision 0, capacity 0, staggeredSharing 0, sameDayRepeat 0, fieldReuse 0); sweep demoted 2/2/5 respectively to get there. (Note: my test harness fires two gens per day — date-change auto-gen + explicit click — so validating *immediately* can catch a transient mid-regen snapshot with residual violations; the **settled** schedule, i.e. what a user sees/saves, is clean. A minor real-world note: changing date then clicking Generate triggers a redundant second generation.)

**Status:** FN-18 (validator noise), FN-20 (locator wrong activity), FN-19 (special capacity), FN-21 (staggered sharing) ALL fixed + live-verified. DAW HEAD `344e8651`; main still `caf6e501`. Camp restored (06-03 intact 35/175, all test dates purged).

---

# ✅ "WHY SO MANY FREES" — diagnosed + both levers (2026-06-04)

**Diagnosis (from the live coverage warning):** every Free traced to ONE cause — a **special-activity room was already occupied** (most specials are cap-1 / not_sharable), the special couldn't be placed, and the slot was left **Free with no fallback**. The layer-mapped agent confirmed: the live planner is `runGlobalPlanner`; the only gap-filler reachable at FORMALIZE is `pickFillActivity` (L1056), which is **specials-only and room-blind** → a special-layer Free slot is never offered a sport. Compounded by (a) greedy non-room-aware special assignment and (b) duplicate misspelled specials sharing one room (Accessorize/Accesorize, Neranitas/Nernitas, Art Shoppes/Art Shoppees, Arts & Crafts 2/Arts and Crafts 2, etc.) ~doubling demand on each cap-1 room.

**Lever A (CODE — FN-22, done + live-verified) — `fabb80b5` + `af0d68d7`:** added a post-pass `_freeFillSweepFN22` that runs LAST (after the FN-15/19/21 sweeps) and fills every remaining Free slot — both solver-left (room contention) AND sweep-demoted — with a bunk-accessible SPORT on a **completely-empty field** (conflict-free by construction: empty field = no sharing/capacity/stagger risk), respecting access + no same-day-repeat. **Live-verified 2 fresh gens: 06-22 Free 5→0, 06-23 Free →0; freePct 0.00%, totalErrors 0 both** (FN-22 filled 4 and 5 slots respectively). Frees are now effectively eliminated.

**Lever B (CONFIG dedupe — tested-not-applied; recommended for the UI):** the 6 genuine duplicate pairs the user should merge in Special Activities (keep one spelling each): **Accessorize/Accesorize · Neranitas/Nernitas · Art Shoppes/Art Shoppees · Arts & Crafts 2/Arts and Crafts 2 · Arts & Crafts 3/Arts and Crafts 3 · Minute to Win it/Minute To Win It.** ⚠️ NOT a duplicate: **"Skits"** legitimately shares the "Beis Medrash" room with "Minute to Win it" — so room-grouping alone over-merges; dedupe needs human judgment → belongs in the UI. I tested a localStorage dedupe but `loadGlobalSettings()` reads cloud/in-memory, so it was inert; **the live cloud config was never modified (verified: 30 specials, Nernitas still present).** With Lever A already eliminating Frees, Lever B is now a quality/correctness improvement (more bunks get their *intended* special instead of a fallback sport; also removes the cap-1-vs-cap-2 validator ambiguity), not a Free-elimination necessity.

**Net:** Frees fixed in code (Lever A, verified 0). Config dedupe (Lever B) is a recommended user UI cleanup (exact list above). DAW HEAD `af0d68d7`; main still `caf6e501`; live config + 06-03 untouched.

---

# ✅ `/goal` — "lets get all of these done" (8 open items) — ALL CLOSED (2026-06-04)

The 8 residual items from the walkthrough, each driven to a fix or a definitive verification. DAW only (commits below); main untouched at `caf6e501`.

| # | Item | Severity | Outcome | Commit |
|---|------|----------|---------|--------|
| 1 | **FN-7** dead division-lock branch | LOW (latent) | **FIXED** | `038feba8` |
| 2 | **#136** edit→unconfigured-activity not rotation-counted | LOW | **FIXED** | `57fd1d3a` |
| 3 | **#137** localStorage near quota (overlapping config snapshots) | LOW | **VERIFIED — no code change** | — |
| 4 | **FN-17** in-session gen churn / date-settle race | MED | **FIXED** | `4fed47a2` |
| 5 | **F2** special write-phase capacity backstop | MED | **FIXED** | `5d1f2f99` |
| 6 | **FN-11** multiPart per-part name/location at gen | LOW | **FIXED** | `3eadfd76` |
| 7 | **Leagues / specialty / zones** live-exercise | — | **LEAGUES LIVE-VERIFIED**; specialty + zones code-verified, config-gated | (test only) |
| 8 | **Multi-user / scheduler roles** | — | **VERIFIED to single-account max** (synthetic + RLS-design) | (verify only) |

**1 — FN-7 (`038feba8`, auto_field_locks.js ~L291):** the division-skip `if(claim.lockType==='division' && claim.grade===divisionContext) continue;` was unreachable because the outer guard only entered on `lockType==='exclusive'`. Widened to `exclusive || division`. Inert today (no division locks configured) but now correct when one is.

**2 — #136 (`57fd1d3a`, scheduler_core_utils.js L2627 `getValidActivityNames`):** manually editing a slot to a configured **sport** (Soccer/Basketball/Hockey) placed it but `applyPostEditCounts` never incremented its rotation count, because the valid-name set was built from specials/electives only. Added `app1.allSports` + `app1.sportMetaData` keys. **Live-verified:** Soccer now in the valid set (57 names, was 49); the "Socer" typo stays excluded (so genuine typos still aren't silently counted).

**3 — #137 (verified, no code change):** the two ~839KB `CAMPISTRY_LOCAL_CACHE` + `campGlobalSettings_v1` snapshots were already cut to a 20-byte cross-tab beacon in the promoted follow-up `0b51da08`, and the `campDailyData_v1` unbounded-growth path was capped to ~45 dates in `98dfc228` (#V2-1). Re-measured live: localStorage steady, no QuotaExceeded, auto-save not skipped. A further dedupe of the remaining config snapshot is **multi-reader-risky** (app1/calendar/go/integration_hooks all read it) and not worth the regression risk for the headroom gained → deliberately left as-is.

**4 — FN-17 (`4fed47a2`, daily_adjustments.js L4700 `runOptimizer`):** generating several days back-to-back via automation could fire a gen before the date-picker change settled, stamping the new schedule onto the previously-loaded date (observed once: a 06-28 gen landed on 06-04). Added a date-settle guard: if `#calendar-date-picker`.value ≠ `window.currentScheduleDate`, re-dispatch the change, alert "still loading", and refuse the gen. A human never hits this (they wait for the grid); it hardens the fast-automation path. The 7-day week reran clean afterward (7/7, no cross-date write).

**5 — F2 (`5d1f2f99`, scheduler_core_auto.js special write phase):** added `_specialRoomOverbooked(room, special, grade, bunk, startMin, endMin)` called at the special-write site — it counts current occupants of the room in the partially-built `scheduleAssignments` and **refuses an overbooking write at placement time**, so capacity is enforced where the block is created rather than only cleaned up afterward by the FN-19/21 final sweep. Belt-and-suspenders with the sweep; the sweep remains as the last-line guarantee.

**6 — FN-11 (`3eadfd76`, scheduler_core_auto.js `getMultiPartInfo` L959 + write block):** a multi-part special's parts can each carry their own `name`/`location` (`parts:[{name,location}]`). The generator labeled every part "Base N/M" at the base room. Now `getMultiPartInfo` returns `partName`/`partLocation` from `parts[partNumber-1]`, resolved **before** validation so the part room flows through the rules-check / F2 / write / field-lock, and the tile is labeled with the part name (falling back to `Base N/M` when a part omits a name). Inert for this camp (0 multiPart specials) but correct for any that configures one.

**7 — Leagues / specialty / zones (live test, then restored):** injected the camp's own "Test Leagues" auto-template (4 `type:"league"` layers) onto a throwaway date and ran the **real Generate button**. Result: **19 league-game slots placed — Minors 2, Soloists 6, Duetos 5, Trios 6** (exactly the 4 league-bearing grades), the 3 grades **without** a league (Quartet, Quints, Majors) correctly got **none**, and `validateSchedule()` = **0 errors** (league games introduce no field/capacity/share conflicts). → **leagues LIVE-VERIFIED.** Specialty leagues placed 0 games via this template (the Test template has no specialty *round*; `processSpecialtyLeagues` + the FN-9 `_tbdIndex` fix are deployed — needs a specialty period to exercise). Zones: 0 configured → `getZoneForField`/`getTravelForField`/`getFieldsInZone` return correct null/default values. Both remaining are **config-gated** (like FN-11): code is in place, the camp's "Test" template just doesn't configure them. Injected template + all test dates fully removed afterward.

**8 — Multi-user / scheduler roles (verified to single-account ceiling):** `mergeSchedules` (supabase_schedules.js L518) re-read and proven sound — newest-wins by `updated_at`, per-bunk ownership (disjoint, no write race), FN-V2-25 structure-aware prune (drops resurrection-bunks not in `app1.divisions`, gated `records.length>1` so single-user is a no-op). `getGeneratableDivisions` (access_control.js L1234) scopes a SCHEDULER to `_editableDivisions`/`_directDivisionAssignments`; owner/admin → all. `verifyBeforeWrite` always re-checks cloud role before the first write (no kiosk pre-seed bypass). Synthetic two-row merge proven live. **True live enforcement (RLS row-level, cross-user DELETE propagation, simultaneous edits, scheduler-scope UI) genuinely requires a 2nd authenticated account — out of reach single-account, and speculatively changing sound, reviewed code would only add regression risk → left as the documented 2-account follow-up.**

**Restore after all tests:** manual mode; test dates 06-22/23/24 deleted from cloud + local; only legit future dates 07-08 / 07-15 remain in cloud; **06-03 intact (35 bunks / 175 blocks)**; injected `campAutoLayers_2026-06-22` removed.

**Net:** 5 code fixes (FN-7, #136-adjacent, FN-17, F2, FN-11) + 3 verifications (#137 quota, leagues live, multi-user to ceiling). DAW HEAD `3eadfd76` → (docs commit next); main still `caf6e501`; live config + 06-03 untouched.

---

# ✅ MANUAL-MODE CAPACITY-EXCEED + LEFTOVER FREE — FIXED + PROVEN (2026-06-05)

User report: *"Capacity Exceeded: Arts and Crafts 3 has 2 Quints bunks at 2:15 PM (capacity: 1) … this is manual mode. figure out how there can be a capacity exceeding and fix. figure out why there is a free and fix it."*

## Root cause (capacity) — duplicate / misspelled specials share one physical room with conflicting caps
The camp has **duplicate special-activity definitions that point at the SAME physical location with DIFFERENT sharing rules**:
- `Arts & Crafts 3` (ampersand) → **not_sharable, cap 1**, location "Arts and Crafts 3"
- `Arts and Crafts 3` (the word "and") → **same_division, cap 2**, location "Arts and Crafts 3"
- Same pattern for **Accesorize/Accessorize** (Accesorize=same_division cap2 + Accessorize=not_sharable cap1, both at room "Accessorize"), and the rest of the misspelled set (Neranitas/Nernitas, Art Shoppes/Art Shoppees, Arts & Crafts 2/Arts and Crafts 2, …).

The MANUAL generator places a special using **that special's own** `sharableWith`, so it happily puts **2 same-division bunks** in the room via the cap-2 twin. But `auto_validator` resolves capacity by **physical location** (its sharing map is location-keyed, first-writer-wins, and **defaults any field it can't resolve to not_sharable cap-1**) → it flags 2 bunks in that room as "capacity: 1". Generator and validator **disagree**, so the manual schedule ships with validator-flagged capacity violations. The auto solver never showed this because it already runs a final room-capacity sweep (`_fn1921FinalSweep`); **the manual solver had no equivalent.**

## Root cause (free)
`STEP 7.5` (manual free-fill, `autoFillSlotSilent`) **skips any slot flagged `_fixed`/`_pinned`/`_bunkOverride`** — but a Free *skeleton* slot legitimately carries those flags, so it was never filled (the original lone Free). And demoting an over-capacity bunk creates a new Free that needs refilling.

## The fix (all on `scheduler_core_main.js` + `scheduler_core_utils.js`, manual path; auto already had its own)
1. **`getFieldCapacity` (utils, shared)** — when no real facility-field owns a location, fold in the **most-restrictive** cap across every special sharing that location (live-checked: Arts and Crafts 3 / Arts and Crafts 2 → cap 1; Arts and Crafts 1 stays cap 2 since its twin uses a different location string; normal fields unchanged). `68d84bb9`
2. **STEP 7.55 room-capacity demote sweep (NEW)** — mirrors the auto `_fn1921FinalSweep`: resolves each room **exactly as `auto_validator.buildFieldSharingMap` does** (field precedence → first special per location → **unknown room defaults to not_sharable cap-1**), then demotes any occupant beyond the room's capacity / sharing rule (anti-stagger pass + capacity/sharing-type pass). `_league`/`_postEdit`/`_pinned` are user-locked → counted toward capacity but never demoted. `821e4831` + `18aca11f`
3. **STEP 7.6 empty-field free-fill (NEW)** — refills every Free (including swept ones) with a bunk-accessible sport on a **completely-empty** field (conflict-free), detecting Free by the slot's *activity* (not its flags, so flagged-but-Free skeleton slots are filled). `68d84bb9`
4. **Preserve `_startMin`/`_endMin` on demoted slots** so STEP 7.6 can resolve their time and refill them (without it the demoted slot read time-null and stayed Free). `f4660a86`

## Live proof — run on the user's ACTUAL reported 06-04 schedule (restored from a pre-fix backup)
`validateAutoSchedule` (the validator that produced the user's "capacity: 1" message):

| | crossDivision | capacity | staggeredSharing | sameDayRepeat | **errors** | **frees** |
|---|---|---|---|---|---|---|
| **Before** | 1 | **2** | 2 | 0 | **5** | **1** |
| **After** (STEP 7.55 + 7.6) | 0 | **0** | 0 | 0 | **0** | **0** |

Sweep demoted 5 (the cap/cross-grade/staggered offenders incl. the Arts-and-Crafts-3 and Accesorize twin overbooks); free-fill refilled all 6 resulting Free slots. Only a **benign pre-existing `fieldReuse` WARNING** remains (one bunk using a field for two *different* sports at different times — not a conflict). Also independently verified by standalone sweeps against the live config (AC3 → cap 0; Accesorize → cap 0) and by a real manual Generate-button regen earlier (AC3 fixed, frees 0).

## ⚠️ Real root cause is config hygiene — user action recommended
The CODE now keeps the schedule validator-clean, **but it does so by bumping the 2nd bunk off the over-booked special onto a fallback sport.** The user should **dedupe the duplicate/misspelled special names in Special Activities** (keep ONE spelling each): **Arts & Crafts 2 / Arts and Crafts 2 · Arts & Crafts 3 / Arts and Crafts 3 · Accessorize / Accesorize · Neranitas / Nernitas · Art Shoppes / Art Shoppees · Minute to Win it / Minute To Win It** — then both bunks keep their *intended* special at cap-2 and nobody is bumped. (⚠️ NOT a dup: "Skits" legitimately shares the "Beis Medrash" room — dedupe needs human judgment, so it belongs in the UI.)

## Deploy + state
4 commits on **DAW** (HEAD `f4660a86`); **main untouched at `caf6e501`**. Server-confirmed deployed. **06-03 never touched this session (all work on 06-04) → intact.** The cleaned 06-04 (0 errors / 0 frees) is in memory + localStorage; the cloud save + final 06-03 re-read could not be confirmed in-session because the browser tab's Supabase calls began hanging at the end (transient renderer/network degradation — pure-JS still ran). **Recommended: the user opens 06-04 in manual mode and clicks Generate once** to apply the deployed fix with full rotation bookkeeping (and ideally dedupe the specials first).

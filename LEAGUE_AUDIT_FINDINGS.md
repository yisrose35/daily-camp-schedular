# League Systems Audit — Confirmed Findings (2026-06-12)

Workflow run wf_106c6399-0cf: 8 dimension finders -> adversarial verifier per finding -> completeness-critic sweep -> verify. 54 findings survived verification (CONFIRMED unless marked PLAUSIBLE).

Scope: leagues.js, specialty_leagues.js, scheduler_core_leagues.js, scheduler_core_specialty_leagues.js + invocation sites (scheduler_core_main/auto, calendar.js, integration_hooks.js, unified_schedule_system.js, display layer).

Numbering: LG-1..LG-54 (severity-sorted).


## HIGH (6)

### LG-1 [CONFIRMED] leagues.js:505
**Load precedence picks the league source with the MOST leagues — a never-cloud-updated localStorage mirror or the legacy app1 mirror can beat fresher cloud data and re-push it to cloud**

- Evidence: loadLeaguesData outside the protection window: `if (fromGlobalCount >= fromLSCount && fromGlobalCount >= fromApp1Count && fromGlobalCount > 0) { loadedData = deepClone(fromGlobal); } else if (fromLSCount >= fromApp1Count && fromLSCount > 0) { loadedData = deepClone(fromLS); } else if (fromApp1Count > 0) { loadedData = deepClone(fromApp1); }` (L505-514). `fromLS` reads `localStorage.getItem('campistryGlobalSettings')` (L468) — repo-wide grep shows that key is written ONLY by leagues.js saveLeague
- Scenario: Camp has 4 leagues. Owner on device B last saved leagues a week ago (campistryGlobalSettings holds 4 leagues w/ old scores). On device A the owner deletes league 'Hockey' and enters new scores in the other 3; cloud + device B's campGlobalSettings_v1 sync to 3 fresh leagues. Device B refocuses the tab -> refreshFromStorage -> fromLSCount=4 > fromGlobalCount=3 -> the STALE localStorage mirror wins: 
- Verifier: Every link in the claimed chain is real in the code, and I can name concrete state that triggers the wrong outcome. 1) COUNT-BASED SELECTION, NO RECENCY: leagues.js loadLeaguesData L503-514 — outside the 5s protection window the winner is purely whichever source has more valid leagues (countValidLeagues L442-454 just counts entries with a name/teams). Both sources carry an updated_at (saveLeagues

### LG-2 [CONFIRMED] integration_hooks.js:708
**Scheduler-role league saves (games/results, leagueHistory, leagueRoundState) are silently discarded before reaching cloud, while the league code logs 'saved to cloud'**

- Evidence: executeBatchSync: `if (!_canWriteCampState()) { ... if (_roleCheckRetries <= 5) {retry} ... log('Skipping camp_state sync — role cannot access camp_state table (changes saved locally)'); _pendingChanges = {}; return; }` (L695-710), and `_canWriteCampState()` returns true only for owner/admin (L163-169) — scheduler is read-only. But access_control.js canEditSetup() L1114-1116 returns true for SCHEDULER, so leagues.js saveLeaguesData passes its RBAC gate (L383-386), writes localStorage, queues the
- Scenario: A scheduler account generates a day containing league periods (a supported multi-scheduler flow). The engine records gamesPerDate/gameLog and FN-58 auto-saves the day's games into leaguesByName — but every cloud write is dropped: results exist only in that scheduler's browser (campistryGlobalSettings / campLeagueHistory_v2). The owner opens the Leagues page on their device: no games for the day. O
- Verifier: Every link in the claimed chain is real. (1) access_control.js:1114-1116 canEditSetup() returns true for SCHEDULER, so leagues.js saveLeaguesData (L383-386) passes its RBAC gate, queues the cloud write via saveGlobalSettings('leaguesByName') (L414) and unconditionally console.logs "[LEAGUES] Data saved to cloud" (L416). (2) scheduler_core_leagues.js saveLeagueHistory (L81-94) queues saveGlobalSett

### LG-3 [CONFIRMED] calendar.js:27
**Specialty history localStorage key mismatch — New Half / Erase All clear the wrong key, stale history resurrects into cloud**

- Evidence: calendar.js:27 `const SPECIALTY_LEAGUE_HISTORY_KEY = "specialtyLeagueHistory_v1";` but the engine's real backup key is scheduler_core_specialty_leagues.js:22 `const SPECIALTY_HISTORY_KEY = "campSpecialtyLeagueHistory_v1";`. startNewHalf does `localStorage.removeItem(SPECIALTY_LEAGUE_HISTORY_KEY)` (calendar.js:582) and eraseAll lists the same wrong key (calendar.js:692), so the engine's `campSpecialtyLeagueHistory_v1` backup survives every reset. The cloud key IS cleared (calendar.js:606 clearClo
- Scenario: Owner clicks 'Start New Half' (or 'Erase All'). Cloud key specialtyLeagueHistory is cleared and the page reloads, but localStorage campSpecialtyLeagueHistory_v1 still holds gamesPerDate/matchupHistory/teamFieldRotation/gameLog from the first half. Next generation: loadSpecialtyHistory sees empty cloud, loads the stale localStorage history, numbers the day 'Game 14' instead of 'Game 1', uses first-
- Verifier: Every step of the claimed chain is verified in the actual code. (1) Key mismatch: calendar.js:27 defines SPECIALTY_LEAGUE_HISTORY_KEY = "specialtyLeagueHistory_v1" but the engine's persistent key is "campSpecialtyLeagueHistory_v1" (scheduler_core_specialty_leagues.js:22); repo-wide grep shows NOTHING in production ever writes "specialtyLeagueHistory_v1" while the engine writes "campSpecialtyLeague

### LG-4 [CONFIRMED] specialty_leagues.js:2397
**Specialty results store never hydrated unless the tab is opened — FN-58s auto-save and date-delete cleanup silently no-op**

- Evidence: Module-local store starts empty (specialty_leagues.js:78 `let specialtyLeagues = {};`) and `loadData()` is ONLY called from `initSpecialtyLeagues` (line 678) and `refreshFromStorage` (line 558); init fires only on tab activation (flow.html:744 `if (id === "specialty-leagues") window.initSpecialtyLeagues?.();`). There is no script-run auto-load — contrast leagues.js:2863 `window.loadLeagueGlobals(); // Auto-load on script run` which hydrates `leaguesByName` for every page. So `SpecialtyLeaguesAP
- Scenario: User loads flow.html, goes straight to the builder, and generates a day containing specialty-league tiles (never clicking the Specialty Leagues tab). The engine schedules games and records history, but the FN-58s call `SpecialtyLeaguesAPI.syncGamesFromGeneration(id, date, entries)` hits an empty store and returns false — the day's games are never written into specialtyLeagues[<id>].games / cloud k
- Verifier: The module-local store `specialtyLeagues` (specialty_leagues.js:78) starts empty and is hydrated ONLY by loadData(), whose only two call sites are refreshFromStorage (line 558) and initSpecialtyLeagues (line 678). refreshFromStorage is reachable only via listeners registered by setupTabListeners() at lines 178/199/222/233 — and setupTabListeners() is called only at line 762, INSIDE initSpecialtyLe

### LG-5 [CONFIRMED] calendar.js:1232
**Scheduler-scoped day delete wipes ALL leagues' results + history for the date (both systems)**

- Evidence: eraseCurrentDailyData's scheduler branch deletes only the scheduler's own divisions' bunks (calendar.js:1144 `const myBunks = getBunksForDivisions(myDivisions); ... deleteBunksFromAllRecords(dateKey, myBunks)`), but then falls through to the UNSCOPED cleanup at 1232-1233: `window.SchedulerCoreLeagues?.cleanupDateFromHistory?.(dateKey); window.SchedulerCoreSpecialtyLeagues?.cleanupDateFromHistory?.(dateKey);`. Regular cleanupDateFromHistory (scheduler_core_leagues.js:2284-2291) iterates `Object.k
- Scenario: Scheduler A owns Juniors; Scheduler B owns Seniors with a Seniors league that played 4 games on 06-15 and has scores entered. A deletes THEIR 06-15 schedule ("Other divisions' data will be preserved"). Seniors' schedule rows survive in the cloud, but cleanupDateFromHistory rolls back the Seniors league's gameLog/teamSports/matchupHistory AND removeAutoGamesForDate deletes the Seniors auto games — 
- Verifier: Every link in the claimed chain is real, with no intervening guard: 1) FALL-THROUGH IS REAL. In calendar.js, eraseCurrentDailyData's scheduler branch (lines 1128-1160) performs the scoped bunk delete (`getBunksForDivisions(myDivisions)` line 1144, `deleteBunksFromAllRecords(dateKey, myBunks)` line 1145) and does NOT return on success — only the cloud-error path returns (1150) and the viewer branc

### LG-6 [CONFIRMED] scheduler_core_main.js:1958
**Manual scoped generation permanently drops every NON-scoped division's league matchups (wipe-without-restore, then wholesale save to cloud)**

- Evidence: runSkeletonOptimizer unconditionally zeroes the division-keyed store AFTER the v17.12 cloud merge: line 1958 `window.leagueAssignments = {};` — but the merge that was supposed to preserve other divisions ran earlier at 1784-1796 (`window.leagueAssignments = mergedLeagues;`) and is discarded. The partial-gen restore path restores ONLY scheduleAssignments (`existingScheduleSnapshot = JSON.parse(JSON.stringify(snapshotSource))` at 1821 snapshots scheduleAssignments alone; executeStep1_5/restoreBack
- Scenario: Owner has a full manual day with league games in Divisions A, B, C (matchups stored in leagueAssignments). They use the generation scope picker to regenerate only Division A. Step 0 deletes the owner's cloud row; 1958 wipes all league data in memory; only Division A's fillBlock re-writes entries; saveSchedule pushes a leagueAssignments map containing ONLY Division A to localStorage and the owner's
- Verifier: Every link in the claimed chain checks out in the actual code. (1) Scoped manual gen is real: daily_adjustments.js:5260-5264 passes the picker's subset as runSkeletonOptimizer's third arg (signature at scheduler_core_main.js:1431). (2) leagueAssignments is DIVISION-keyed (scheduler_core_main.js:462-468, 3408-3413), so the STEP-0 partial wipe at 1501/1511 (`delete window.leagueAssignments?.[bunk]`)


## MED (32)

### LG-7 [CONFIRMED] scheduler_core_leagues.js:31
**loadLeagueHistory prefers the cloud-synced copy whenever it is non-empty, with no recency check against the campLeagueHistory_v2 backup — a stale cloud row shadows newer local history at generation time**

- Evidence: `const global = window.loadGlobalSettings?.() || {}; if (global.leagueHistory && Object.keys(global.leagueHistory).length > 0) { ... console.log("[RegularLeagues] ✅ Loaded history from cloud"); return history; } // Fallback to localStorage const raw = localStorage.getItem(LEAGUE_HISTORY_KEY);` (L30-44). The fallback is reached ONLY when the cloud copy is empty; there is no timestamp comparison, even though saveLeagueHistory (L81-94) always writes BOTH stores, so campLeagueHistory_v2 can legitima
- Scenario: Day 1: owner generates league games offline; saveLeagueHistory writes campLeagueHistory_v2 + queues the KV write; the tab is closed before reconnect, so the cloud 'leagueHistory' row still holds Day-0 state while localStorage holds Day-1. Day 2: page reloads, hydration fills _localCache.leagueHistory from the (older, non-empty) cloud row; the owner generates the next league day -> loadLeagueHistor
- Verifier: The quoted code is verbatim real: loadLeagueHistory (scheduler_core_leagues.js L27-79) returns the hydrated cloud copy whenever it is non-empty (L31-40) and reads the campLeagueHistory_v2 localStorage backup ONLY when that copy is empty (L43-44). There is no recency check — and none is possible, because saveLeagueHistory (L81-94) stores the raw history JSON with no timestamp in either store. CAVEA

### LG-8 [CONFIRMED] leagues.js:166
**Realtime refresh is dead code: 'campistry-remote-change' is never dispatched anywhere, so a focused Leagues tab keeps a stale in-memory store and its next save clobbers remote edits (whole-key write, no merge)**

- Evidence: leagues.js registers `window.addEventListener('campistry-remote-change', handleRemoteChange)` (L160-167, reacting to key 'leaguesByName'/'leagueRoundState'), but a repo-wide grep finds only the 3 listeners (leagues.js, special_activities.js, specialty_leagues.js) and ZERO dispatch sites. The actual remote path — integration_hooks postgres_changes handler L1959-1977 -> hydrateFromCloud -> setLocalSettings — updates only _localCache/IDB and dispatches 'campistry-cloud-hydrated', which leagues.js d
- Scenario: Two admins enter game results concurrently. Admin A (device A) saves scores for League X; realtime delivers the change to device B and _localCache updates, but B's Leagues tab is focused the whole time, so no focus event fires and B's in-memory leaguesByName still lacks A's scores. Admin B then types a score for League Y -> saveLeaguesData serializes B's ENTIRE stale store and upserts the leaguesB
- Verifier: Every element of the claim verifies against the actual code. (1) Dead listener: leagues.js L160-167 registers window.addEventListener('campistry-remote-change', handleRemoteChange) keyed to 'leaguesByName'/'leagueRoundState', but a repo-wide grep finds ONLY the 3 listener sites (leagues.js:166, specialty_leagues.js:236, special_activities.js:84) and zero dispatch sites; the one dynamic dispatcher 

### LG-9 [CONFIRMED] integration_hooks.js:1815
**Hydration merge granularity loses newer local league data: cloud-wins is decided by the MAX updated_at across ALL KV rows, and the local-wins branch compares only the local OVERALL stamp — leagueHistory/leaguesByName regress or clobber per-key**

- Evidence: `for (const row of kvRows) { ... if (Number.isFinite(t) && t > maxUpdated) maxUpdated = t; } if (maxUpdated > 0) { cloudUpdatedAt = new Date(maxUpdated).toISOString(); cloudState.updated_at = cloudUpdatedAt; }` (L1681-1697) then `} else { mergedState = cloudState; log('Using cloud state (newer)'); }` (L1814-1817) — local keys NEWER than their individual cloud rows are discarded wholesale whenever ANY unrelated cloud row is newer than the local overall stamp. Conversely the local-newer branch use
- Scenario: Device A generates league games; its 'leagueHistory'/'leaguesByName' KV upsert is lost (offline close — _pendingChanges is memory-only, L661-676 — or the scheduler-role drop). Local mirror + IDB on A hold the newer history (overall local stamp T1). Device B meanwhile edits any unrelated setting at T2>T1 (cloud max becomes T2). A's next boot: cloudTime(T2) > localTime(T1) -> mergedState = cloudStat
- Verifier: All three legs of the claim are verifiably present in C:/Users/yisro/daily-camp-schedular/integration_hooks.js. (1) LOCAL SIDE HAS ONLY ONE STAMP. updateLocalSetting (L605-610) bumps a single overall `current.updated_at = new Date().toISOString()` on EVERY key write; grep for any per-key local timestamp store (keyTimes/perKeyTime/etc.) returns nothing. So `localTime` (L1751) is the time of the de

### LG-10 [CONFIRMED] calendar.js:645
**startNewHalf specialty standings/playoff reset mutates the store but its save function does not exist — reset never persists**

- Evidence: calendar.js:641-645: `const _specLeagues = window.specialtyLeagues || {}; Object.values(_specLeagues).forEach(function(lg) { if (lg) { lg.standings = {}; if (lg.playoff) lg.playoff = { enabled: false, rounds: [] }; } }); if (typeof window.saveSpecialtyLeaguesData === 'function') window.saveSpecialtyLeaguesData();` — `saveSpecialtyLeaguesData` is referenced nowhere else in the repo and specialty_leagues.js never exports it (its saver is the private `saveData()`; exports are initSpecialtyLeagues/r
- Scenario: Owner runs 'Start New Half' expecting standings wiped. Specialty league standings/playoff are reset only on the in-memory object (or not at all if the tab was never opened), no save is queued because window.saveSpecialtyLeaguesData is undefined, the page reloads, and the cloud key 'specialtyLeagues' still carries every team's first-half W/L/T and playoff bracket — second half starts with last half
- Verifier: Every link in the claimed failure chain checks out against the actual code. (1) calendar.js:634-646 (startNewHalf, owner branch) mutates standings/playoff on window.leaguesByName and window.specialtyLeagues in memory, then guards both saves with typeof checks. (2) A full-repo grep for saveSpecialtyLeaguesData finds ONLY the calendar.js:645 reference — it is defined nowhere. specialty_leagues.js's 

### LG-11 [CONFIRMED] scheduler_core_specialty_leagues.js:1238
**resetHistory cleared on one device resurrects from another device's localStorage backup (empty-cloud fallback)**

- Evidence: `SpecialtyLeagues.resetHistory` (lines 1233-1243) removes the LOCAL backup and writes cloud `{}`: `localStorage.removeItem(SPECIALTY_HISTORY_KEY); ... window.saveGlobalSettings('specialtyLeagueHistory', {});`. But loadSpecialtyHistory (line 32) treats an empty cloud object as 'no cloud data' — `if (global.specialtyLeagueHistory && Object.keys(global.specialtyLeagueHistory).length > 0)` — and falls back to `localStorage.getItem(SPECIALTY_HISTORY_KEY)` (line 46). Any OTHER browser/device still hol
- Scenario: Owner resets specialty history on laptop A (cloud key now {}). Co-director's laptop B still has campSpecialtyLeagueHistory_v1 in localStorage; B generates the next day's schedule → loadSpecialtyHistory finds empty cloud, silently loads B's stale local history, numbers games from the old count, and saveSpecialtyHistory pushes the old history back to the cloud — A's reset is reverted without any war
- Verifier: The claim is accurate on every link of the chain. (1) SpecialtyLeagues.resetHistory (scheduler_core_specialty_leagues.js:1233-1243) removes the localStorage backup ONLY on the resetting device and writes the cloud key to {} via saveGlobalSettings('specialtyLeagueHistory', {}). (2) loadSpecialtyHistory:32 gates the cloud branch on Object.keys(global.specialtyLeagueHistory).length > 0, so the delibe

### LG-12 [CONFIRMED] scheduler_core_leagues.js:1734
**Out-of-order generation replays/skips round-robin rounds; updateFutureSchedules renumbers labels but never re-derives matchups**

- Evidence: Line 1734: `const roundIndex = (gameNumber - 1) % fullSchedule.length;` where gameNumber comes from calculateStartingGameNumber (lines 104-120), which sums gamesPerDate entries with `date < currentDate`. When a later date is generated FIRST, both dates compute the same base. updateFutureSchedules (lines 137-356) then only rewrites label strings — line 246 `dData[dSlot].gameLabel = \`Game ${correctNum}\`;`, lines 307-313 rewrite `_gameLabel`/`sport`/`_allMatchups` text — it never re-runs generate
- Scenario: matchup_variety league, empty history. User generates 06-15 first: base=0 -> Game 1 -> fullSchedule[0] pairings placed. User then generates 06-12: base=0 again -> Game 1 -> fullSchedule[0] -> the IDENTICAL pairings are placed on both dates (every pair plays twice back-to-back, violating the strict-RR guarantee). updateFutureSchedules relabels 06-15 to 'Game 2' but its matchups stay round-0; the ne
- Verifier: All four legs of the claim hold in C:/Users/yisro/daily-camp-schedular/scheduler_core_leagues.js. (1) calculateStartingGameNumber (lines 104-120) sums history.gamesPerDate entries strictly `date < currentDate`, so with empty history generating 06-15 first gives base=0, and a subsequent generation of 06-12 ALSO gives base=0 (06-15 is not < 06-12). (2) Round selection is purely `roundIndex = (gameNu

### LG-13 [CONFIRMED] scheduler_core_leagues.js:1398
**offCampusCounts never rolled back on regen/delete — FN-54 fixed teamSports/matchupHistory but not trip counts**

- Evidence: Line 1398 `ocRecordTrips(history, league.name, dh.offCampus.teams);` increments `history.offCampusCounts[league|team]` (lines 1108-1111) on every engine run. rollbackDayRecords (lines 408-431) subtracts only teamSports and matchupHistory from the gameLog; gameLog entries (line 405) record only {t1,t2,sport,g} — WHICH teams traveled is never logged, so neither the day-reset (line 1216), Leagues.resetDayRecords (2242), nor cleanupDateFromHistory (2275) can subtract trips. Trip count dominates away
- Scenario: Off-campus league, teamsPerDay=4. Generate 06-12: teams A,B,C,D go away, each gets offCampusCounts+1. User regenerates 06-12 (or deletes the date): teamSports/matchupHistory are rolled back exactly, but A-D keep their phantom trip. The regen's ocSelectGroups now strongly avoids A-D (score trip*10000), sending E,F,G,H away — who also keep +1 when the day is regenerated again. After a few regens eve
- Verifier: The claim is accurate in every particular. (1) ocRecordTrips (scheduler_core_leagues.js:1108-1111) increments the flat, date-less counter history.offCampusCounts['league|team'] and is called at line 1398 inside processRegularLeagues' off-campus double-header pre-processing; saveLeagueHistory(history) at line 2081 persists it every engine run. (2) The FN-54 gameLog entry (line 405) records only {t1

### LG-14 [CONFIRMED] scheduler_core_leagues.js:543
**sport_variety chooser systematically starves one team of games with odd team counts (no bye-fairness memory)**

- Evidence: choosePairingsForSportVariety (515-563) scores matchings by fresh-sport coverage with bye pairs skipped (line 534 `if (pair[0] === '__BYE__' ...) continue;` — before the meet accumulation at 535), tie-breaking on fewest prior meetings (line 543 `if (fresh > bestFresh || (fresh === bestFresh && meet < bestMeet))` — strict comparison keeps the FIRST best in generatePerfectMatchings' deterministic enumeration order, lines 499-513, which always appends '__BYE__' last). Nothing tracks who was byed; t
- Scenario: 5-team league (T0..T4), plentiful sports, season start. Game 1: all matchings tie at fresh=4, meet=0 -> first enumerated matching wins: [T0,T1],[T2,T3],[T4,BYE]. Game 2: matchings byeing T4 with zero rematches still exist ([T0,T2],[T1,T3],[T4,BYE]) and enumerate before any zero-meet matching where T4 plays -> T4 byed again. Game 3: [T0,T3],[T1,T2],[T4,BYE] is still fresh=4/meet=0 -> T4 byed a THIR
- Verifier: All four pillars of the claim verified in C:/Users/yisro/daily-camp-schedular/scheduler_core_leagues.js. (1) generatePerfectMatchings (L499-513) appends '__BYE__' last and enumerates deterministically (rem[0] paired with rem[1], rem[2], ... in order), so the first matching for [T0..T4] is always [T0,T1],[T2,T3],[T4,BYE]. (2) In choosePairingsForSportVariety, bye pairs are skipped at L534 BEFORE th

### LG-15 [CONFIRMED] leagues.js:859
**League rename orphans ALL engine history (numbering restarts at Game 1, day rollback permanently broken)**

- Evidence: Lines 856-867: `makeEditable(title, function (newName) { ... delete leaguesByName[league.name]; league.name = newName; leaguesByName[newName] = league; ...})` renames only the config object. Engine history is keyed by league name everywhere: teamSports `${leagueName}|${team}` (line 359), matchupHistory `${leagueName}:pair` (379), gamesPerDate[leagueName] (106), gameLog[leagueName] (401) in scheduler_core_leagues.js — none are migrated. The day-reset (1207-1220) and rollbackDayRecords look up `hi
- Scenario: League 'Soloists' has 5 game-dates in history. User double-clicks the title and renames it 'Solo League'. Next generation: calculateStartingGameNumber('Solo League') sums an empty map -> Game 1 again (labels and roundIndex restart; fullSchedule[0] replayed despite 5 played rounds); sport/matchup variety and the rematch-sport caveat see zero history -> repeats permitted immediately. Worse: regenera
- Verifier: The rename handler (leagues.js:856-863) only updates league.name, the leaguesByName key, and selectedLeagueName, then saveLeaguesData() — it never touches the engine history stored separately in globalSettings.leagueHistory (cloud-synced via saveLeagueHistory, scheduler_core_leagues.js:81-94). That history is keyed by league NAME everywhere: gamesPerDate[leagueName] (L106-108), gameLog[leagueName]

### LG-16 [CONFIRMED] scheduler_core_leagues.js:1213
**Day-reset in-play test wipes a multi-division league's history + saved scores when only ONE of its divisions is regenerated without it**

- Evidence: Lines 1209-1213: a league is 'in play' (and gets rollbackDayRecords + gamesPerDate delete + FN-58 result replacement) if ANY block's division intersects league.divisions — `return Object.keys(td.byDivision).some(function (d) { return (league.divisions || []).includes(d); })` — or `(league.divisions || []).some(function (d) { return _genDivs.has(d); })`. If the league is then never rescheduled in this run (its blocks aren't in the partial scope), lines 2090-2107 call syncGamesFromGeneration with 
- Scenario: League B spans divisions D1 and D2; its games for 06-12 (with scores already typed into Game Results) were placed across both. A scheduler does a partial regen of D1 only (context.generatedDivisions=['D1']) after removing B's league tile from D1's skeleton. B matches in-play via D1 -> its 06-12 gameLog/gamesPerDate are rolled back and FN-58 clears its auto games+scores for 06-12 — yet D2's grid (n
- Verifier: Every link in the claimed chain is real code, verified end-to-end: 1) SCOPED BLOCKS: In a partial regen, scheduler_core_main.js skips skeleton items for non-selected divisions entirely (line 2899-2902: "★★★ PARTIAL GEN CHECK ★★★ if (allowedDivisionsSet && !allowedDivisionsSet.has(String(divName))) return;"). So with a D1-only scope, D2's league blocks never enter leagueBlocks/schedulableSlotBlock

### LG-17 [CONFIRMED] scheduler_core_specialty_leagues.js:663
**Field-rotation tie-break is unreachable dead code — teamFieldRotation never influences field choice**

- Evidence: In assignMatchupsToFieldsAndSlots (L656-668): `if (currentGames < maxGames && currentGames < minGames) { const rotationScore = getFieldRotationScore(...); if (currentGames < minGames || (currentGames === minGames && rotationScore > 0)) { minGames = currentGames; bestField = field; } }`. The inner branch `currentGames === minGames && rotationScore > 0` can never be true because the OUTER guard already requires `currentGames < minGames` (strict). So the inner condition is always satisfied by its f
- Scenario: League with fields [Court A, Court B], 2 matchups/day, ties at count 0 every slot: the matchup sorted first by waitScore always gets Court A, second always Court B. With stable waitScore ties (all teams at slotOrder 1 when matchups <= fields), the round-robin pair order maps to the same fields day after day — a team can play Court A every single game all season while teamFieldRotation dutifully re
- Verifier: Read assignMatchupsToFieldsAndSlots in C:/Users/yisro/daily-camp-schedular/scheduler_core_specialty_leagues.js L632-703. The outer guard at L660 requires `currentGames < minGames` STRICTLY; nothing mutates currentGames/minGames before the inner test at L663, so the inner condition's first disjunct `currentGames < minGames` is always true and the tie-break disjunct `(currentGames === minGames && ro

### LG-18 [CONFIRMED] scheduler_core_specialty_leagues.js:878
**Division names containing '_' are truncated by key.split('_') — specialty league silently never schedules, and day-reset still wipes the date's records**

- Evidence: Blocks are grouped with `const key = `${block.divName}_${block.startTime}`;` (L861) then unpacked with `const [divName, startTime] = key.split('_');` (L878). A division named e.g. 'Junior_Boys' yields divName='Junior', startTime='Boys'. League lookup then fails both paths (L895 `(l.divisions || []).includes(divName)` and L907 `l.divisions.includes(divName)`) → 'No enabled league for division Junior' → continue. Meanwhile the day-reset (L824-837) used the CORRECT name (`specialtyBlocks.map(b => b
- Scenario: Camp names a division 'Junior_Boys' and gives it a specialty league tile. Every generation: the reset wipes the league's history/results rows for the date, the processing loop fails to resolve the league (wrong divName after split), the block is left to the generic filler, and shared-game/lock keys (`${league.id}_${startTime}` L920, divisionTimes lookup L1052) are also corrupted. The specialty lea
- Verifier: The claimed chain is fully present in scheduler_core_specialty_leagues.js. (1) L860 joins divName and startTime with '_' and L878 unpacks via key.split('_') with two-element destructuring, so any division name containing '_' is truncated at its first underscore ('Junior_Boys' -> divName='Junior', startTime='Boys'). (2) Both league-resolution paths compare the truncated name against configured divi

### LG-19 [CONFIRMED] scheduler_core_leagues.js:1407
**indoorRequirement saved+enforced on normal league days but silently ignored on Away-Games (offCampus) days**

- Evidence: Signature is `function assignMatchupsToFieldsAndSports(matchups, availablePool, leagueName, history, slots, schedulingPriority, leagueRules)` (L888) and the normal path passes `{ indoorRequirement: league.indoorRequirement, indoorCounts: indoorCountsByLeague[league.name] }` (L1892-1895). The off-campus double-header path calls it with only 6 args, omitting leagueRules entirely: `var g1Off = assignMatchupsToFieldsAndSports(dh.offCampus.game1, fp1.filter(...), league.name, history, s1, priority);`
- Scenario: League has Advanced Settings: Indoor Court Requirement '>= 1 indoor game per day' AND Away Games enabled with 2 consecutive league periods. Generation routes the whole day through the off-campus double-header path (auto-pair detection L1341-1359) → all 4 game assignments (home and away groups, both games) pick fields with zero indoor steering; teams can end the day with 0 indoor games and no warni
- Verifier: The off-campus double-header path in scheduler_core_leagues.js calls assignMatchupsToFieldsAndSports with only 6 args (L1407-1410: matchups, pool, league.name, history, slots, priority), omitting the 7th leagueRules param that the normal path supplies at L1885-1896 ({indoorRequirement, indoorCounts}). Inside the assigners, leagueRules===undefined makes _indoorConstraintDir (L916-917 'if (!req || !

### LG-20 [CONFIRMED] scheduler_core_leagues.js:1383
**Chinuch fully bypassed (teams double-booked) when Away Games handles the day's league periods**

- Evidence: Chinuch precompute (L1228-1338) assigns every team a chinuch period across ALL league time-keys, including ones later consumed by the off-campus pair. But the off-campus path builds matchups from the FULL roster: `var leagueTeams = league.teams || []; ... var fullSched = generateRoundRobinSchedule(leagueTeams);` (L1383-1388) — no filter against window.chinuchSchedule (contrast the normal loop: `activeTeams = leagueTeams.filter(t => !chinuchTeamsHere.includes(t))` L1580). It then marks all the le
- Scenario: League with chinuch.enabled AND offCampus.enabled, two back-to-back league periods (auto-paired at L1341-1359, so this is the every-day path for such leagues). Team 'Cobras' is assigned chinuch at period 1; the off-campus path still pairs Cobras into Game 1 and Game 2 matchups. Result: Cobras plays both games AND its chinuch session silently never appears anywhere on the schedule — the chinuch set
- Verifier: Whole-file grep for 'chinuch' proves zero references inside the off-campus pre-processing block (scheduler_core_leagues.js L1361-1481) or any oc* helper (L1068-1124); chinuch is written ONLY in the normal per-time-key loop (chinuch-only blocks L1600-1632, _chinuchLines L2026-2050). The precompute (L1243) gates only on chinuch.enabled+league.enabled — no offCampus exclusion — and allPeriodKeys (L12

### LG-21 [CONFIRMED] scheduler_core_leagues.js:1110
**Away-trip fairness counts (offCampusCounts) inflate on every regeneration — not covered by the FN-54 day-reset rollback**

- Evidence: `ocRecordTrips`: `teams.forEach(function(t) { history.offCampusCounts[leagueName+'|'+t] = (history.offCampusCounts[leagueName+'|'+t]||0) + 1; });` (L1108-1111), called unconditionally per generation at L1398 (before field assignment even succeeds). The FN-54 day-reset (L1203-1221) calls rollbackDayRecords (L408-431) which restores teamSports + matchupHistory from gameLog and deletes gamesPerDate[dayId] (L1217-1219), but offCampusCounts is never decremented anywhere — the only reset is the manual
- Scenario: User generates a day with Away Games (teams A,B,C,D travel), then regenerates the same date twice to fix something else. A-D are now charged 3 trips each in history.offCampusCounts while everyone else has ≤1 → next game day ocSelectGroups (trip*10000) hard-avoids A-D, the UI chips show inflated trip numbers, and the 'some teams travel each game day' rotation is permanently skewed without any user-
- Verifier: Every link in the claimed chain is real in the code. (1) ocRecordTrips (scheduler_core_leagues.js L1108-1111) increments history.offCampusCounts per away team with no date-keying, called unconditionally at L1398 inside the off-campus double-header loop of processRegularLeagues — and it fires BEFORE field assignment (L1407-1410) and before the g1All.length>0 placement gate (L1414), so trips are cha

### LG-22 [CONFIRMED] scheduler_core_leagues.js:2027
**chinuch.bunkFacilities room is display-only — facility never reserved, can be double-booked by the main solver**

- Evidence: The per-team facility chosen in Advanced Settings (leagues.js L1467-1496, dropdown from window.getFacilities()) is only ever interpolated into display strings: `const fac = league.chinuch?.bunkFacilities?.[t] || 'Chinuch'; return `${t} — Chinuch (${fac})`;` (L2027-2029, same at L1602-1604 for chinuch-only periods). The facility name never becomes a pick.field and is never locked: every GlobalFieldLocks call in the file covers league game fields or playoff reservedActivities only (L1434, L1458, L
- Scenario: Team 'Red' is assigned chinuch in facility 'Beis Medrash' at the 10:00 league period. The general solver, seeing 'Beis Medrash' unlocked and unused, places another bunk's special activity there at 10:00 → two groups in one room. The user configured a per-team room precisely to prevent this; the setting is honored on paper (printout text) but not as a resource constraint.
- Verifier: CONFIRMED for manual mode (the decisive case); auto mode has a partial incidental mitigation the claim missed. Manual: the per-team chinuch facility is only ever interpolated into _allMatchups display strings (scheduler_core_leagues.js L2027-2029 and L1602-1603); pick.field is always 'League: <name>' (L1616/L2042), and fillBlock (scheduler_core_main.js L588/L631) registers slot usage under that ps

### LG-23 [CONFIRMED] specialty_leagues.js:435
**validateLeague allowlist silently wipes engine-read advanced options (conferences, allowInterConference, interConferencePriority, gamesPerFieldSlot, sports[]) — and no UI exists to set them**

- Evidence: validateLeague builds a NEW object with only whitelisted keys (specialty_leagues.js:435-447): `const validated = { id, name, divisions, sport, fields, teams, enabled, standings, games, playoff }` — nothing else survives. But the engine destructures and enforces four additional league options: scheduler_core_specialty_leagues.js:432-439 `const { id, teams, conferences, allowInterConference, interConferencePriority } = league;` (conference round-robins at 445-485, inter-conference games gated by `
- Scenario: Camp sets gamesPerFieldSlot:1 and two conferences on a specialty league via console/import/older build -> engine schedules conference round-robins, 1 game per field. Staff later opens the Specialty Leagues tab and renames a team -> saveData persists every league through validateLeague -> conferences/allowInterConference/interConferencePriority/gamesPerFieldSlot deleted from cloud. Next generation 
- Verifier: Every link in the claimed chain is real. (1) validateLeague (specialty_leagues.js:416-473) constructs a NEW object whose only keys are id/name/divisions/sport/fields/teams/enabled/standings/games/playoff — conferences, allowInterConference, interConferencePriority, gamesPerFieldSlot, and sports are silently dropped. (2) The engine genuinely consumes those keys from the RAW store: scheduler_core_sp

### LG-24 [CONFIRMED] scheduler_core_specialty_leagues.js:539
**Field time-rule filter evaluates the slot window using an arbitrary division's times, not the division being scheduled**

- Evidence: assignMatchupsToFieldsAndSlots line 539: `const _divSlots = window.divisionTimes?.[Object.keys(window.divisionTimes || {})[0]] || [];` then `const _slotStart = _divSlots[slots[0]]?.startMin; const _slotEnd = _divSlots[slots[slots.length - 1]]?.endMin;` — it takes the FIRST key of window.divisionTimes, but slot indices map to different clock times per division (that is the whole point of divisionTimes). The function receives only (matchups, league, history, slots) — divName is never passed in, ev
- Scenario: Seniors' slot 3 runs 2:00-2:45pm; the first division in divisionTimes (e.g. Juniors) has slot 3 at 11:00-11:45am. A specialty league for Seniors uses 'Main Court', which has timeRule unavailable 11:00am-12:00pm. The filter tests 11:00-11:45 -> overlap -> Main Court dropped from the pool even though the actual game is at 2pm (league starved of its field). Inverse case: field unavailable 2-3pm passe
- Verifier: The claim is accurate on every element. (1) assignMatchupsToFieldsAndSlots (line 506) receives only (matchups, league, history, slots) — no division — even though its sole real caller (line 1025, inside processSpecialtyLeagues) has divName in scope from line 878 (`const [divName, startTime] = key.split('_')`). (2) Line 539 resolves the slot indices against `Object.keys(window.divisionTimes)[0]` — 

### LG-25 [CONFIRMED] specialty_leagues.js:1045
**Orphaned/stale entries in league.fields are invisible and unremovable in the UI yet still scheduled and globally locked by the engine (parallel-list drift vs facilities config)**

- Evidence: The Fields card renders chips ONLY for fields currently mapped to the league's sport (specialty_leagues.js:1045-1047): `const sportFields = globalFields.filter(f => f && f.name && (Array.isArray(f.activities) ? f.activities.includes(league.sport) : false))`. Any league.fields entry that no longer qualifies (field deleted in Facilities, renamed, or sport removed from its activities) gets NO chip — there is no way to see or deselect it. validateLeague does not validate fields against the facility 
- Scenario: Owner deletes 'Old Hockey Rink' in Facilities (or renames it). The specialty league that had it selected keeps 'Old Hockey Rink' in league.fields forever; the Edit Setup card shows only the surviving fields all looking deselected-or-correct, so staff cannot remove the ghost. Next generation assigns real matchups to the nonexistent rink ('Team A vs Team B — Old Hockey Rink' printed on schedules) an
- Verifier: All four links of the claimed chain check out against real code, and no guard elsewhere breaks it. 1) UI invisibility/unremovability — C:/Users/yisro/daily-camp-schedular/specialty_leagues.js:1045-1061: the Fields card builds chips ONLY from the current facility config filtered by sport (`globalFields.filter(f => ... f.activities.includes(league.sport))`); a `league.fields` entry whose field was 

### LG-26 [CONFIRMED] scheduler_core_leagues.js:2317
**Erase-ALL-schedules never removes auto games from either results store — ghost games keep counting in standings**

- Evidence: eraseAllDailyData (owner branch, calendar.js:1427-1428) calls only `window.SchedulerCoreLeagues?.clearAllGamesPerDate?.(); window.SchedulerCoreSpecialtyLeagues?.clearAllGamesPerDate?.();`. Both implementations wipe ONLY history.gamesPerDate: regular `for (const leagueName of Object.keys(history.gamesPerDate)) { history.gamesPerDate[leagueName] = {}; }` (scheduler_core_leagues.js:2322-2324), specialty identical (scheduler_core_specialty_leagues.js:1402-1404). Neither calls LeaguesAPI.removeAutoGa
- Scenario: Owner clicks "Delete ALL Daily Schedules" (flow.html eraseAllSchedulesBtn -> eraseAllDailyData). Every schedule for every date is deleted, yet the Leagues and Specialty Leagues Game Results pages still list every auto-imported game with scores, standings still count W/L/T from schedules that no longer exist, and the stale games (importedFrom 'auto') persist to cloud via saveLeaguesData/saveData. P
- Verifier: eraseAllDailyData (calendar.js:1427-1428) calls only SchedulerCoreLeagues.clearAllGamesPerDate() and SchedulerCoreSpecialtyLeagues.clearAllGamesPerDate(). Both implementations wipe ONLY history.gamesPerDate (regular: scheduler_core_leagues.js:2322-2324; specialty: scheduler_core_specialty_leagues.js:1403-1405) and never call LeaguesAPI.removeAutoGamesForDate / SpecialtyLeaguesAPI.removeAutoGamesFo

### LG-27 [CONFIRMED] calendar.js:1303
**Scheduler erase-all branch does ZERO league cleanup (no gamesPerDate, no gameLog rollback, no auto-game removal)**

- Evidence: The scheduler branch of eraseAllDailyData (calendar.js:1303-1375) deletes their divisions' bunks from every cloud record and localStorage date, then `return`s before reaching the owner branch's `clearAllGamesPerDate` calls at 1427-1428. The branch contains no call to clearAllGamesPerDate, cleanupDateFromHistory, resetDayRecords, or removeAutoGamesForDate for either system.
- Scenario: A scheduler erases all their divisions' schedules across all dates. Every league whose divisions belong to that scheduler keeps: all auto-saved games in the results store (standings unchanged), all gameLog/teamSports/matchupHistory entries, and all gamesPerDate counts — so the next generation numbers games as if the deleted history still exists (Game 12 instead of Game 1) and variety/rematch logic
- Verifier: The scheduler branch of eraseAllDailyData (calendar.js 1303-1375) deletes the scheduler's bunks from every cloud daily_schedules record (1334-1345), every localStorage date (1350-1359), and window globals (1361), then returns at 1374 — before the owner branch's league cleanup at 1427-1428 (clearAllGamesPerDate for both regular and specialty) and leagueRoundState reset (1434-1437). Grep confirms ca

### LG-28 [CONFIRMED] leagues.js:2806
**Label renumbering never propagates to results store -> doubleheader pair loses BOTH entered scores on regen**

- Evidence: Score-preservation fallback is gated: `if (!old && oldPairCount[pk] === 1 && newPairCount[pk] === 1) old = oldByPair[pk];` (leagues.js:2806; specialty identical at specialty_leagues.js:2436). Meanwhile updateFutureSchedules (scheduler_core_leagues.js:137-330) renumbers gameLabels ONLY in allDailyData leagueAssignments/scheduleAssignments — it never updates the results store's `league.games[].gameLabel` nor `history.gameLog[lg][date][].g`. So after an earlier date is deleted, a future date's stor
- Scenario: League plays a doubleheader: pair A|B plays Game 3 AND Game 4 on 06-20 (engine supports this — `_doubleHeaderPairId`), user enters both scores. User deletes the 06-18 league day -> cleanupDateFromHistory + updateFutureSchedules renumber 06-20's schedule to Game 2/Game 3, but results store still says Game 3/Game 4. User regenerates 06-20: new labels "Game 2|A|B"/"Game 3|A|B" miss the old "Game 3|A|
- Verifier: Every link in the claimed chain checks out in the actual code, with one precision correction on the outcome. (1) Renumbering never reaches the results store or gameLog. updateFutureSchedules (scheduler_core_leagues.js:137-356) rewrites gameLabels ONLY inside allDailyData — leagueAssignments (`dData[dSlot].gameLabel = \`Game ${correctNum}\`` L246) and scheduleAssignments (`e._gameLabel`/`e.sport`/

### LG-29 [CONFIRMED] specialty_leagues.js:2140
**Specialty 'Import from Schedule' replaces existing game wholesale -> wipes scores already entered on the FN-58s auto game**

- Evidence: importGamesFromSchedule builds `newGame` with `matches: gameData.matchups.map(m => ({ teamA, teamB, scoreA: null, scoreB: null }))` (L2124-2129), finds an existing game by `g.date === currentDate && g.gameLabel === gameData.gameLabel` (L2135-2137), then `league.games[existingIdx] = newGame;` (L2140) — full replacement with null scores. The regular-leagues import instead MERGES and preserves scores (leagues.js:2474-2487 only pushes matchups not already present). The FN-58s sync now auto-creates t
- Scenario: Generation auto-saves specialty Game 2 for today (FN-58s); counselor enters all 3 match scores. Someone later clicks the legacy 'Import from Schedule' button (still rendered, specialty_leagues.js:1873-1875 analog): the existing Game 2 is replaced by a null-score copy, saveData persists to cloud, all of today's entered specialty results are destroyed with no warning — while the same click in regula
- Verifier: specialty_leagues.js importGamesFromScheduleV2 builds newGame with all scores null (L2124-2129), finds an existing game by (date, gameLabel) (L2135-2137), and on hit does `league.games[existingIdx] = newGame;` (L2140) — wholesale replacement with no score preservation and no confirmation, then saveData() (L2149) persists to localStorage + cloud. The FN-58s sync (syncGamesFromGeneration, L2429-2431

### LG-30 [CONFIRMED] scheduler_core_main.js:3369
**Manual builder rainy-day skip bypasses league day-reset — ghost games persist (auto builder has the reset, manual doesn't)**

- Evidence: scheduler_core_main.js:3369-3377: `const _skipLeagues = isRainyDayModeActive() || window.isRainyDay === true; if (_skipLeagues) { ... window.leagueAssignments = {}; } else { ... processSpecialtyLeagues(leagueContext); ... processRegularLeagues(leagueContext); }` — on a rainy day NEITHER engine is invoked and nothing else resets the date. The auto builder explicitly handles the engines-never-run case: scheduler_core_auto.js:20480-20490 `} else { // ★ FN-54: no league blocks this run... window.Sch
- Scenario: Monday is generated normally in manual mode: league Game 5 is recorded into history.gameLog/teamSports/matchupHistory/gamesPerDate and FN-58 auto-saves Game 5 to the Leagues results store. It then rains and the user regenerates Monday in rainy-day mode. The new Monday schedule has zero league games, but: (1) the results page still lists Game 5 for Monday, (2) gamesPerDate still counts it so Tuesda
- Verifier: Verified the full mechanism in the actual code. (1) Manual rainy skip: scheduler_core_main.js:3369-3377, inside window.runSkeletonOptimizer (defined L1431, the manual generation entry, aliased runOptimizer at L4209), sets `_skipLeagues = isRainyDayModeActive() || window.isRainyDay === true` and on true skips Steps 4/5/5.5 entirely, only clearing the in-memory display map `window.leagueAssignments 

### LG-31 [CONFIRMED] scheduler_core_specialty_leagues.js:272
**Specialty updateFutureSchedules never received the regular engine's FN-10 + time-key-dedup fixes — wrong game numbers on future dates**

- Evidence: Per-bunk renumber, scheduler_core_specialty_leagues.js:272-273: `const slotGameIndex = Array.from(processedSlots).indexOf(`${i}`); const correctNum = gamesBeforeThisDate + (slotGameIndex >= 0 ? slotGameIndex : 0) + 1;` — indexes the leagueAssignments-derived set with a bunk-schedule array index. This is the exact pattern the regular engine documents as broken and replaced (scheduler_core_leagues.js:260-266: '★ FN-10: per-day sequential game counter... (The old code reused processedSlots... via i
- Scenario: Specialty league has games on 06-16 (2 games) and a schedule already exists for 06-17. User regenerates 06-15 → updateFutureSchedules renumbers 06-16/06-17. (a) Two divisions share the one 06-16 game at the same time but different slot indices → counted as two games → 06-17 labels inflate by 1. (b) In the per-bunk pass, slotGameIndex resolves to -1 → every per-bunk specialty entry on the date is r
- Verifier: The specialty mirror provably lacks both fixes that the regular engine documents and carries. (1) Dedup: scheduler_core_specialty_leagues.js:217 keys processedSlots by raw slot index (`const slotKey = `${slotIdx}`;`), while scheduler_core_leagues.js:212-219 was explicitly fixed to key by normalized startTime ("★★★ FIX: Deduplicate by actual time, not slot index ★★★ Different divisions can have dif

### LG-32 [CONFIRMED] scheduler_core_specialty_leagues.js:1153
**Specialty playoff TBD placeholders are recorded into history and auto-saved as 'TBD vs TBD' result games (regular engine explicitly skips recording for TBD)**

- Evidence: scheduler_core_specialty_leagues.js:1153 calls `updateHistoryAfterScheduling(league, assignments, history, currentDate, gameLabel);` unconditionally — including when `_playoffIsTBD` is true and assignments are FN-9 TBD placeholders (teamA/teamB = 'TBD', lines 990-1005). updateHistoryAfterScheduling (709-737) then writes teamFieldRotation['id|TBD'], lastSlotOrder['id|TBD'], matchupHistory['id|TBD|TBD'], and gameLog entries {tA:'TBD', tB:'TBD'}; the FN-58 sync (1174-1191) reads that gameLog and pu
- Scenario: A specialty league is in playoff round 1 and the skeleton gives it two slots the same day (game 2 = forecast next round). Generation succeeds, but the Specialty Leagues results page now shows an auto-saved game '<League> Playoff R2 TBD' with match 'TBD vs TBD', team 'TBD' accrues field-rotation/lastSlotOrder history, and matchupHistory gains an 'id|TBD|TBD' key. Rollback cleans it on regen, but un
- Verifier: Every link in the claimed chain checks out in the actual code. (1) scheduler_core_specialty_leagues.js:980-1005: in playoff mode with todayGameIndex>0, matchups are built as {teamA:'TBD', teamB:'TBD', _playoffTBD:true, _tbdIndex:k} and _playoffIsTBD=true. (2) These are NOT info-only: assignMatchupsToFieldsAndSlots explicitly supports them (FN-9 keying at 632-639, `matchup._playoffTBD ? 'TBD-'+_tbd

### LG-33 [CONFIRMED] scheduler_core_leagues.js:1348
**Off-campus double-header auto-detect indexes masterLeagues by name — silently dead in the auto builder, which passes an array**

- Evidence: scheduler_core_leagues.js:1348: `var league = lName && masterLeagues[lName];` (the only place in the engine that indexes masterLeagues by name instead of using the `Array.isArray(masterLeagues) ? masterLeagues : Object.values(...)` normalization used at 1206/1241/1365/1507). The manual builder passes the by-name object (scheduler_core_loader.js:557 `masterLeagues: window.masterLeagues || {}`, where leagues.js:25 sets `window.masterLeagues = leaguesByName`), so it works there. The auto builder pa
- Scenario: A league has offCampus enabled (zone + teamsPerDay) and the auto builder has two consecutive league layers. In manual mode the engine schedules the away/home double-header and records trips. In auto mode the same config generates two ordinary single games — no zone split, no offCampusCounts trip recording — with zero warning, so trip fairness counters drift between builders.
- Verifier: Every link of the claim holds against the actual code. (1) scheduler_core_leagues.js:1348 indexes masterLeagues by league NAME: `var league = lName && masterLeagues[lName];` followed by 1349 `if (!league || !league.offCampus?.enabled) return;`. This is inside the off-campus auto-detect loop (1340-1359), which is the ONLY place in the entire codebase that SETS `_doubleHeaderPairId` for auto-built b

### LG-34 [PLAUSIBLE] scheduler_core_leagues.js:411
**rollbackDayRecords has no null-entry guard, and the manual builder invokes the engines without try/catch — one corrupted gameLog entry aborts the whole manual generation**

- Evidence: scheduler_core_leagues.js:411-412: `entries.forEach(function (e) { if (e.sport) {` — a null/undefined element in history.gameLog[league][date] (e.g., from a partial cloud write or a bad merge of the leagueHistory KV blob) throws TypeError; same in the specialty mirror (scheduler_core_specialty_leagues.js:753-754 `if (e.field)`). Elsewhere the same author guards this exact shape: getPairSports at :490 uses `if (e && e.sport ...)`. The FN-54 day-reset IIFE that calls rollbackDayRecords (scheduler_
- Scenario: history.gameLog['Soloists']['2026-06-15'] contains [null, {t1:...}] after an interrupted cloud save. User clicks Generate in MANUAL mode for 06-15: the day-reset throws on `e.sport`, the exception propagates out of processRegularLeagues uncaught, and the entire generation aborts mid-run — after specialty leagues have already placed games and locked fields — leaving a half-generated day. The identi
- Verifier: Every code assertion in the claim verifies exactly. (1) scheduler_core_leagues.js:411-412 `entries.forEach(function (e) { if (e.sport) {` has no null-entry guard — a null element in history.gameLog[league][date] throws TypeError; the specialty mirror scheduler_core_specialty_leagues.js:753-754 (`if (e.field)`) is identical; the same author DOES guard this shape in getPairSports at :490 (`if (e && 

### LG-35 [CONFIRMED] scheduler_core_auto.js:22878
**Auto partial gen restores the STALE pre-gen league snapshot OVER the freshly generated matchups (division-keyed map filtered by bunk names), then persists the stale map**

- Evidence: Step 0 clones the division-keyed map: line 682 `_preservedLeagueData = structuredClone(window.leagueAssignments || {});` (the preceding wipe at 668 `delete window.leagueAssignments?.[bunk];` is a no-op because keys are division names, so the clone still contains the SCOPED division's old game). Step 5 restore: lines 22875-22878 `for (const [bunk, data] of Object.entries(_preservedLeagueData)) { if (!_partialGenBunks.has(bunk)) { ... window.leagueAssignments[bunk] = data; } }` — `bunk` is actuall
- Scenario: Auto day exists with Trios' league showing 'Game 4' (T1 vs T2). User scope-regenerates only Trios. The engine derives 'Game 5' (T3 vs T1), records it in leagueHistory.gameLog/gamesPerDate, and FN-58 auto-saves Game 5 into the results store — but the restore clobbers leagueAssignments back to Game 4's matchups and that stale map is what is saved to campDailyData + cloud. Grid/print/locator show Gam
- Verifier: Every step of the claimed mechanism is in the code, in C:/Users/yisro/daily-camp-schedular/scheduler_core_auto.js unless noted. 1) `window.leagueAssignments` is DIVISION-keyed: the league writeback (lines 20271-20275) writes `window.leagueAssignments[lb.divName][lb.startMin] = { matchups, gameLabel, sport, leagueName }` and the comment calls it the "authoritative source for grid renderer". Reads 

### LG-36 [PLAUSIBLE] unified_schedule_system.js:2608
**getLeagueMatchups fabricates sequential team pairings and renders them as real matchups whenever the slot-index lookup misses — and it always misses auto's startMin-keyed entries**

- Evidence: Lines 2595-2607 try only `leagues[divName]?.[slotIdx]` plus a ±2 SLOT-INDEX fuzz. The auto builder standardizes on startMin keys (scheduler_core_auto.js:20273 `window.leagueAssignments[lb.divName][lb.startMin] = ...` and 20276-20287 explicitly DELETES slot-index-keyed duplicates `if (!isNaN(n) && n < 200 ...) delete ...`), so for an auto day the lookup falls through to lines 2608-2619, which invent matchups from config: `for (let i = 0; i < teams.length - 1; i += 2) { ... displayMatchups.push({ 
- Scenario: User opens the Daily Schedule (unified) view of an auto-generated day, or any day whose leagueAssignments entry was lost (see the two partial-gen findings). The league cell shows 'Pirates Game' with Team1 vs Team2, Team3 vs Team4 — static naive pairings that match neither the engine's round-robin output nor the games recorded in history/results. Staff print or read matchups that were never schedul
- Verifier: The fabrication mechanism is 100% real: getLeagueMatchups (unified_schedule_system.js:2593-2623) tries the slot-index key, then a ±2 numeric fuzz, then INVENTS sequential pairings from masterLeagues config (teams[i] vs teams[i+1], labeled `${league.name} Game`, always applicableLeagues[0], regular leagues only) and both renderers display them identically to real matchups — fabrication even suppres

### LG-37 [CONFIRMED] unified_schedule_system.js:4349
**Post-edit league-field guards treat startMin keys as slot indices — league field usage is invisible on auto days, allowing the field picker / cascade claim to double-book a league's field**

- Evidence: _getLeagueFieldsInTimeRange (4346-4350): `for (const [slotIdx, entry] of Object.entries(divSlots)) { ... const idx = parseInt(slotIdx, 10); const slotInfo = dTimes[idx]; if (!slotInfo) continue; }` — for auto-generated days the keys are startMins (e.g. '650'), so `dTimes[650]` is undefined and EVERY league entry is skipped; the function returns an empty set. Its own doc comment says it exists precisely because 'GlobalFieldLocks may not be initialized during post-edit' (4392-4393). The cascade-cl
- Scenario: On a reloaded auto-generated day (GlobalFieldLocks not rebuilt), a user edits a bunk's 3:00pm activity. findFieldsForActivity lists Hockey Rink as OPEN even though the leagueAssignments matchups say 'T1 vs T2 @ Hockey Rink' at 3:00pm, and claimFieldForBunk's league-conflict block (4989: 'BLOCKED by league game') never triggers. The edit is accepted and two groups arrive at the rink at the same tim
- Verifier: All three legs of the claim verify against real code. (1) Auto-generated days key leagueAssignments by startMin, not slot index: scheduler_core_auto.js:20273 writes window.leagueAssignments[lb.divName][lb.startMin] = {matchups,...} (also 20224), and lines 20281-20287 actively DELETE slot-index keys (Number(k) < 200) — the code standardizes auto days on startMin keys like '650'. (2) Both post-edit 

### LG-38 [CONFIRMED] camper_locator.js:74
**Camper locator team matching uses bidirectional substring inclusion — numeric team labels misroute campers (team '1' matches '10 vs 4' first)**

- Evidence: findTeamMatchup (66-77) iterates matchups in order and returns the FIRST fuzzy hit before later matchups get an exact check: `if (tA === teamStr || tB === teamStr) return norm; if (tA.includes(teamStr) || tB.includes(teamStr) || teamStr.includes(tA) || teamStr.includes(tB)) return norm;`. The file's own header documents the canonical matchup format as numeric teams: 'Regular leagues store matchups as strings: "1 vs 4 @ Newcomb Cage (Newcomb)"' (lines 32-34). '10'.includes('1') is true, so any le
- Scenario: League has teams 1-10. Today's matchups in stored order: ['10 vs 4 @ Court A', '2 vs 9 @ Court B', '1 vs 7 @ Court C']. A parent asks where a camper on team 1 is; the loop tests matchup[0]: exact fails, but tA='10'.includes('1') → returns it. Locator confidently displays 'Court A - Basketball' with '10 vs 4' while the camper is actually at Court C — wrong location reported with no warning, for the
- Verifier: findTeamMatchup (camper_locator.js:66-77) is a single-pass loop: for each matchup in stored order it runs the exact-equality check and then immediately the bidirectional substring check, returning on the first hit. There is no exact-scan-all-matchups-first pass, no word-boundary or length guard. So with teams labeled '1'..'10' (the file's own header at line 33 documents the canonical regular-leagu


## LOW (16)

### LG-39 [CONFIRMED] scheduler_core_leagues.js:1121
**resetOffCampusHistory rebuilds the entire 'leagueHistory' cloud key from loadGlobalSettings() instead of loadLeagueHistory(), and skips the localStorage backup — can wipe gamesPerDate/matchupHistory/gameLog cloud-side**

- Evidence: `window.resetOffCampusHistory = function(ln) { var h = ((window.loadGlobalSettings?.()||{}).leagueHistory||{}); if (ln) {...} else { h.offCampusCounts={}; } window.saveGlobalSettings?.('leagueHistory', h); };` (L1120-1124). Unlike every other history writer it bypasses loadLeagueHistory's localStorage fallback: if _localCache has no leagueHistory yet (early boot reads the stripped campGlobalSettings_v1 snapshot, which deliberately deletes leagueHistory — integration_hooks setLocalSettings L532 —
- Scenario: User opens the console right after page load (before IDB preload/cloud hydration populates _localCache.leagueHistory) and runs resetOffCampusHistory() to clear off-campus trip counts -> saveGlobalSettings('leagueHistory', {offCampusCounts:{}}) batch-syncs and overwrites the camp's entire league history row in camp_state_kv; on the next load the cloud copy is 'non-empty' (it has the offCampusCounts
- Verifier: Every link of the claim is borne out by the code. (1) resetOffCampusHistory (scheduler_core_leagues.js L1120-1124) builds the history object from loadGlobalSettings().leagueHistory instead of loadLeagueHistory(), so it skips the campLeagueHistory_v2 localStorage fallback (loadLeagueHistory L44) and never updates that backup key, while saveLeagueHistory (L81-94) always writes both — split-brain con

### LG-40 [CONFIRMED] specialty_leagues.js:606
**saveData's 'immediate localStorage' race protection writes a legacy key that specialty's own loader never reads (dead-letter; weaker than regular)**

- Evidence: saveData (specialty_leagues.js:605-611) writes `const lsKey = 'campistryGlobalSettings'; ... lsData.specialtyLeagues = dataToSave; localStorage.setItem(lsKey, ...)` then calls `window.saveGlobalSettings?.("specialtyLeagues", dataToSave)` (line 618). But specialty's loadData (lines 480-481) reads ONLY `window.loadGlobalSettings?.()` which is backed by `_localCache`/`campGlobalSettings_v1`/IDB (integration_hooks.js:59 `LOCAL_STORAGE_KEY: 'campGlobalSettings_v1'`, getLocalSettings at 484-499) — the
- Scenario: saveGlobalSettings is unavailable or throws (e.g., script-load-order issue or batch sync dropped by the role gate after 5 retries): the user's score entry appears saved (console logs '✅ Data written to localStorage') but the only durable copy went to a key nothing reads; the next loadData/refreshFromStorage pulls the previous state from campGlobalSettings_v1/IDB and the scores vanish. Regular leag
- Verifier: Every element of the claim is verifiable in code. (1) specialty_leagues.js saveData lines 602-615 writes specialtyLeagues into localStorage key 'campistryGlobalSettings' as its "FIX v2.1: Write to localStorage immediately (prevents race conditions)" and logs "✅ Data written to localStorage". (2) Specialty's only read paths — loadData line 480 (`const g = window.loadGlobalSettings?.() || {}`) and r

### LG-41 [CONFIRMED] scheduler_core_leagues.js:1540
**Off-campus double-header marks the league handled for the WHOLE day — a 3rd league period gets no matchups**

- Evidence: After the OC pre-processor schedules the consecutive pair it sets `offCampusScheduled[league.name] = { handled: true };` (1478). The normal per-time loop then skips the league at EVERY remaining time key: lines 1540-1543 `if (offCampusScheduled[league.name]?.handled) { console.log(' Skipping ...'); continue; }`. Only the two paired blocks were marked processed (1472-1477); blocks at other times stay unprocessed and never receive matchups.
- Scenario: An offCampus-enabled league has 3 league periods on 06-12 (e.g. 10:00, 11:00, 3:00). The auto-detector (1341-1359) pairs 10:00+11:00 as the double-header; the 3:00 block is skipped by line 1540 -> no assignments, no fillBlock -> the 3:00 league tile is left as a raw/Free placeholder for every participating bunk, and gamesPerDate records 2 games though the skeleton asked for 3.
- Verifier: The off-campus pre-processor marks only the two paired blocks processed (scheduler_core_leagues.js:1472-1477) but sets a league-wide flag offCampusScheduled[league.name]={handled:true} (1478) after recording +2 games (1471). The normal per-time loop's per-league guard (1540-1543) then skips that league at EVERY remaining time key, including a 3rd league period whose blocks were never processed. Th

### LG-42 [CONFIRMED] scheduler_core_leagues.js:325
**updateFutureSchedules per-bunk renumber inspects only the FIRST bunk key and updates by raw slot index across divisions**

- Evidence: Lines 268-326: `for (const bunk of Object.keys(assignments)) { ... break; }` — the unconditional `break` at line 325 ('Only iterate bunks once to find slots, then break') means only the first bunk in scheduleAssignments is scanned for league entries; the cross-bunk update writes to `bSchedule[i]` (lines 303-317) at the SAME index for all bunks. The sibling leagueAssignments path explicitly fixed this class of bug by keying on normalized times (lines 212-221) because 'Different divisions can have
- Scenario: Future date 06-15 has league entries; the first key in scheduleAssignments is a bunk from a division NOT in the league (or whose league game sits at a different slot index than other divisions'). Renumbering after generating 06-12 fixes the leagueAssignments labels but the per-bunk copies for some/all bunks keep the old 'Game 1' label -> per-bunk grid/print shows a different game number than the d
- Verifier: Read updateFutureSchedules in scheduler_core_leagues.js (lines 137-356; function is live — called at lines 2124, 2268, 2306). The per-bunk renumber block (lines 268-326) iterates Object.keys(assignments) but has an unconditional `break` at line 325 ("Only iterate bunks once to find slots, then break"), so only the FIRST bunk key is ever scanned for league entries. When that first bunk yields a mis

### LG-43 [CONFIRMED] scheduler_core_leagues.js:1669
**Multi-game-day sport exclusion only excludes each team's LAST sport, contradicting its stated intent for 3+ games**

- Evidence: Lines 1661-1678, comment says 'exclude sports already used by this league EARLIER today', but the implementation takes only the most recent entry per team: line 1668-1670 `var hist = history.teamSports[k] || []; if (hist.length > 0) _prevSports.add(hist[hist.length - 1]);`. teamSports holds the full season history with today's earlier games appended at the end, so for game 3 only game-2 sports are excluded; game-1 sports are eligible again (the per-pair FN-57 caveat only blocks repeats for the S
- Scenario: A league with 3 league slots on one day, sport_variety: Team A plays Basketball (Game 1), Soccer (Game 2). For Game 3 the pool excludes only Soccer; A can be assigned Basketball again the same day against a new opponent — exactly the same-day repeat this filter was added to prevent (it works for the common 2-slot day, silently degrades at 3+).
- Verifier: The multi-game-day exclusion at scheduler_core_leagues.js L1664-1678 builds _prevSports from only `hist[hist.length - 1]` per team, i.e., each team's single most recent sport. history.teamSports is the full chronological season aggregate (recordTeamSport L363-367 is a plain push), and within a multi-slot day each game's sports are appended at L1981-1982 BEFORE the next game's pool is built — so at

### LG-44 [CONFIRMED] scheduler_core_specialty_leagues.js:637
**KNOWN SUSPECT (FN-9 'TBD dedup'): FIXED — but TBD placeholders still pollute history (double-push) and Game Results**

- Evidence: The dedup fix IS live: L637-639 `const matchupKey = matchup._playoffTBD ? `TBD-${matchup._tbdIndex != null ? matchup._tbdIndex : (matchup._playoffSport || '')}` : `${matchup.teamA}-${matchup.teamB}`;` with `_tbdIndex: k` stamped at L1003 — each TBD forecast placeholder reserves its own field. RESIDUAL: TBD assignments still flow into updateHistoryAfterScheduling (L1153). With teamA===teamB==='TBD', keyA===keyB so L724-725 `history.teamFieldRotation[keyA].push(game.field); history.teamFieldRotati
- Scenario: Playoff league with 2 specialty slots in one day: slot 2 emits TBD forecast placeholders (L990-1005). After generation, the Game Results panel shows score-entry rows for 'TBD vs TBD' under label 'X Playoff R2 TBD'; history carries phantom `id|TBD` rotation/slot entries. recalcStandings is unaffected ('TBD' not in league.teams, guarded at specialty_leagues.js L2256), so impact is UI noise + dead hi
- Verifier: Every link in the claimed chain is real in the code. (1) TBD placeholders: scheduler_core_specialty_leagues.js L990-1005 emits {teamA:'TBD', teamB:'TBD', _playoffTBD:true, _tbdIndex:k} when a playoff league has a 2nd+ league slot in one day (todayGameIndex>0, L979). (2) The FN-9 dedup fix IS live at L637-639 (matchupKey = `TBD-${_tbdIndex}` for placeholders), so the FIXED half of the claim is accu

### LG-45 [CONFIRMED] scheduler_core_specialty_leagues.js:297
**updateFutureSchedules renumbers bunk-grid labels from only the FIRST bunk — silently skipped when that bunk has no specialty entries**

- Evidence: The scheduleAssignments renumber pass (L252-298) is `for (const bunk of Object.keys(assignments)) { ... for (let i = 0; i < bunkSchedule.length; i++) { ... } break; }` — the unconditional `break;` at L297 means only the first array-valued bunk is ever scanned (it is used as the iteration template; updates at L279-292 do touch all bunks at slot i). If that first bunk belongs to a division that doesn't play the specialty league, its schedule has no `entry._isSpecialtyLeague` entries (guard L258) a
- Scenario: Schedules exist for 06-20; user then generates 06-15 with 2 specialty games → updateFutureSchedules(06-15) must shift 06-20's 'Game 5' → 'Game 7'. The league-row UI entry is renumbered, but if Object.keys(assignments)[0] is a Minors bunk and the league belongs to Seniors, every bunk tile on 06-20 keeps the stale '<League> Game 5' label (field/_activity/_gameLabel at L282-284 untouched) — grid and 
- Verifier: Read updateFutureSchedules (scheduler_core_specialty_leagues.js L141-328). The scheduleAssignments renumber pass iterates `for (const bunk of Object.keys(assignments))` but ends its body with an unconditional `break;` (L297), so exactly the first array-valued bunk is scanned as the slot template (the L254 `continue` only skips non-array values). If that first bunk's division doesn't play the speci

### LG-46 [CONFIRMED] scheduler_core_specialty_leagues.js:347
**lastSlotOrder: never-played default deprioritizes waiting teams, and values survive date-delete/day-reset**

- Evidence: getWaitPriorityScore (L343-354): `const slotA = lastSlotOrder[keyA] || 1; ... const scoreA = (slotA - 1) * 50;` — a team with NO record (new team, or one whose matchup was trimmed by the capacity cut at L596-603) defaults to slot 1, i.e. score 0, the LOWEST wait priority, tying with teams that just played first. A team cut from an over-subscribed day therefore gains no priority for the next day. Also, per the design note at L748 'lastSlotOrder is overwrite-only and needs no rollback': rollbackDa
- Scenario: 8-team league, field capacity 3 games/day: one matchup is trimmed (L602 slice). The two cut teams keep default slot 1 → waitScore 0 → they tie at the bottom again next day instead of being promoted, so the same teams can be repeatedly starved. Separately, deleting yesterday's schedule leaves yesterday's slot orders driving today's wait priority — a heuristic skew only, no data corruption.
- Verifier: Both mechanisms are real in C:/Users/yisro/daily-camp-schedular/scheduler_core_specialty_leagues.js. (1) getWaitPriorityScore (L343-354) defaults a missing lastSlotOrder entry to slot 1 via `|| 1`, yielding score (1-1)*50 = 0 — the minimum wait priority. lastSlotOrder is written ONLY at L727-728 inside updateHistoryAfterScheduling, per assigned game, so a team that was never scheduled (new team, o

### LG-47 [CONFIRMED] scheduler_core_specialty_leagues.js:37
**conferenceRounds is dead state; matchupHistory is write-only in this engine (no rematch/variety awareness)**

- Evidence: `history.conferenceRounds = history.conferenceRounds || {};` appears only at initialization sites (L37, L50, L60, L77) — grep confirms it is never read or written anywhere else in the repo. Conference round selection is instead derived per call: `const currentRound = (gameNumber - 1) % Math.max(1, roundRobin.length);` (L453) and `const interRound = (gameNumber - 1) % Math.max(1, Math.max(conf1Teams.length, conf2Teams.length));` (L468). Similarly matchupHistory is recorded (L732-733) and rolled b
- Scenario: No direct misbehavior — the round-robin index does rotate pairings. But anyone (or any future feature) assuming conferenceRounds tracks per-conference progress, or that specialty leagues avoid rematches via matchupHistory, is wrong: if gamesPerDate entries are deleted for a mid-season date (cleanupDateFromHistory), gameNumber shifts and the round-robin index silently replays/skips rounds with no m
- Verifier: Both assertions verified by exhaustive grep + reading the enclosing functions. (1) conferenceRounds appears ONLY at init/normalization sites (scheduler_core_specialty_leagues.js L37, L50, L60, L77, plus the empty-shape mirror in tests/specialty_gamelog_sim.js:63); it is never incremented or read anywhere in the repo — conference round selection is derived per call from gameNumber via modulo (L453,

### LG-48 [CONFIRMED] scheduler_core_leagues.js:299
**KNOWN SUSPECT 'future-relabel wrong-Set dup game#' — VERIFIED FIXED (FN-10)**

- Evidence: The old bug (per-bunk relabel indexed the time_-keyed `processedSlots` Set with a bare slot index, so every game on a future date got the same number) is gone. updateFutureSchedules now keeps a dedicated sequential counter: `let bunkGameIndexWithinDay = 0;` (L266) and `const correctNum = gamesBeforeThisDate + bunkGameIndexWithinDay + 1;` (L299) with the FN-10 comment at L260-265 documenting the old wrong-Set failure. The leagueAssignments copy independently uses `gameIndexWithinDay` with time-no
- Scenario: Schedule day N+1 before day N, then generate day N: future day N+1's games are renumbered chronologically and per-bunk labels now advance Game k, Game k+1 (previously all got the identical number). Status: fixed; reported here only to close out the suspect — see separate finding for two NEW residual defects in the same function.
- Verifier: The claim (that the old wrong-Set duplicate-game-number bug is fixed) is accurate. In updateFutureSchedules (scheduler_core_leagues.js), the per-bunk scheduleAssignments relabel block now has a dedicated dedup set `processedBunkSlots` keyed by bare slot index (L259, L287-289 — correct keying for that set) and a dedicated sequential counter `let bunkGameIndexWithinDay = 0;` (L266), incremented per 

### LG-49 [CONFIRMED] scheduler_core_auto.js:20301
**Auto builder chinuch writeback indexes chinuchSchedule by BUNK key though it is keyed by TEAM — divergent from manual builder which removed this exact lookup**

- Evidence: Auto writeback: `if (window.chinuchSchedule?.[leagueName]?.[bk] != null && Number(window.chinuchSchedule[leagueName][bk]) === lb.startMin) { ... bunkFacilities?.[bk] ... }` (scheduler_core_auto.js L20301-20304, duplicated in the fallback path L20353-20356) where `bk` is a per-bunk grid key. But the engine builds chinuchSchedule keyed by TEAM label: `bunkSchedule[shuffled[teamIdx]] = Number(allPeriodKeys[p])` / `bunkSchedule[team] = ...` (scheduler_core_leagues.js L1291, L1329). scheduler_core_ma
- Scenario: Normal configs (team names like 'Cobras' ≠ bunk keys) → dead code, auto and manual agree (chinuch shown via matchup lines only). But in a camp that names teams after bunks (e.g. team 'Bunk 5' = bunk key 'Bunk 5'), AUTO mode replaces that bunk's league slot with a dedicated `_isChinuch` block (field = facility) while MANUAL mode leaves the league block — same config, different schedules per builder
- Verifier: All three legs of the claim verified by direct read. (1) Auto writeback indexes window.chinuchSchedule[leagueName][bk] where bk is a per-bunk grid key: scheduler_core_auto.js L20295 iterates Object.entries(pbs) with pbs = window._perBunkSlots[lb.divName] (bunk-name-keyed — unified_schedule_system.js L6129 writes _perBunkSlots[String(bunkName)], and the same bk indexes window.scheduleAssignments[bk

### LG-50 [CONFIRMED] specialty_leagues.js:1019
**Deselecting the Sport hides the Fields card but keeps league.fields enforced — engine still schedules matchups and hard-locks those fields with sport label 'League'**

- Evidence: Sport chip handler (1018-1022): `league.sport = isActive ? null : act; if(!isActive) league.fields = [];` — fields are cleared only when SELECTING a new sport; deselecting (sport -> null) leaves league.fields populated. The Fields card is then not rendered at all (line 1030: `if (league.sport) { ... }`), so the surviving selection is invisible. The engine has no sport gate: it schedules whenever the league is enabled and division-matched, using `league.fields` as the pool (scheduler_core_special
- Scenario: Staff clicks the active 'Basketball' chip to 'turn off' the league's sport, sees the Fields card disappear, and assumes the league is unconfigured/inert. The skeleton tile is still there, so the next generation schedules a full game on the old basketball courts, labels it sport 'League', and locks both courts away from every other division at that slot — driven entirely by configuration the UI no 
- Verifier: Every element of the claim matches the actual code. (1) UI: specialty_leagues.js sport-chip handler (lines 1018-1023) sets `league.sport = isActive ? null : act` and clears `league.fields` ONLY on the `!isActive` (selecting-new-sport) branch — deselecting the active chip leaves `league.fields` fully populated, and `saveData()` persists that orphaned array. (2) UI: line 1030 `if (league.sport) {` g

### LG-51 [CONFIRMED] specialty_leagues.js:581
**Config edits mutate the in-memory store BEFORE the RBAC gate in saveData — non-editor roles get phantom edits that render as saved but never persist**

- Evidence: saveData opens with the permission gate (581-584): `if (window.AccessControl?.canEditSetup && !window.AccessControl.canEditSetup()) { console.warn('Save blocked...'); return; }` — but every config handler mutates the shared `specialtyLeagues` object FIRST and re-renders from it: division chip 992-993 (`league.divisions.push(divName)` then `saveData()`), sport chip 1019, field chip 1055-1056, team add 1109-1111, enabled toggle 820-821, rename 861-862, score entry 1881-1895 (also sets match.winner
- Scenario: A scheduler-role user (canEditSetup=false) opens Specialty Leagues, adds team 'Sharks', enters tonight's scores, and sees the standings table update and '✓ Saved' style UI respond. Nothing was written locally or to cloud. They close the tab believing results are recorded; the owner later finds the league with no scores. Worse, until that user's tab refreshes, their in-memory store diverges from cl
- Verifier: The mutate-before-gate mechanism is exactly as claimed, verified line-by-line in specialty_leagues.js. (1) saveData (579-584) is the ONLY persistence choke point and its RBAC gate silently returns with just a console.warn — no UI feedback, no rollback of the in-memory store. (2) Every config/score handler mutates the shared `specialtyLeagues` object FIRST, then calls saveData, then re-renders FROM

### LG-52 [CONFIRMED] scheduler_core_specialty_leagues.js:889
**Renaming a specialty league orphans existing skeleton tile picks — tiles silently fall back to the division's first enabled specialty league**

- Evidence: Skeleton tiles pin their league BY NAME, and the engine honors the pick by name only (889-897): `const _pickedLeagueName = (blocks.find(b => b && b.leagueName) || {}).leagueName || null; let league = _pickedLeagueName ? Object.values(specialtyLeaguesConfig).find(l => ... l.name === _pickedLeagueName && (l.divisions || []).includes(divName)) : null;` with an explicit fallback when the name no longer matches (901-909): 'tile pick ... isn't a valid specialty league here — using division default' ->
- Scenario: A division has two specialty leagues, 'ABBL' (created first) and 'Hockey Cup'. Mid-season the owner renames 'Hockey Cup' to 'Stanley Cup'. Every existing 'Hockey Cup' tile now fails the name match and falls back to the division default — the next generation schedules ABBL games in Hockey Cup's slots: ABBL gets double game numbers that day, Stanley Cup's round-robin stalls, and FN-58 auto-saves the
- Verifier: All three legs of the claim verify against real code. (1) The specialty engine honors a tile's pick by NAME only: scheduler_core_specialty_leagues.js:889-896 matches blocks' leagueName against l.name (no ID match, even though the store/history are keyed by league ID), and on mismatch lines 901-909 log one console line then silently fall back to the division's FIRST enabled specialty league in Obje

### LG-53 [CONFIRMED] specialty_leagues.js:2457
**Specialty sync/remove never refreshes an open Games UI; stale rows write scores into detached match objects**

- Evidence: SpecialtyLeaguesAPI.syncGamesFromGeneration ends with `recalcStandings(league); saveData();` (L2456-2457) and removeAutoGamesForDate with `saveData();` (L2482) — neither re-renders the detail pane. leagues.js has the `_refreshGamesUIIfShowing` mechanism for exactly this (L2748-2756, called at 2828 and 2854); specialty has no analog even though it has renderDetailPane. Score inputs capture `match` by closure (`match.scoreA = newScoreA;` L1881) — after a sync replaced `league.games`, that object i
- Scenario: User has the Specialty Leagues Game Results section open while a regeneration runs in the same tab. The list keeps showing the pre-regen games; the user types a score into a stale row -> onblur writes to a match object no longer inside league.games and calls saveData, so the input looks accepted but vanishes on the next render. Regular leagues refresh in place; specialty silently drops the entry.
- Verifier: All four legs of the claim check out in the real code. (1) SpecialtyLeaguesAPI.syncGamesFromGeneration ends at specialty_leagues.js L2456-2457 with recalcStandings(league); saveData(); and removeAutoGamesForDate ends at L2482 with saveData(); — neither calls renderDetailPane (L839) or renderGameEntryUI (L1523), while leagues.js has the exact analog _refreshGamesUIIfShowing (L2748-2756) invoked at 

### LG-54 [CONFIRMED] unified_schedule_system.js:4005
**saveModifiedBunksToCloud overlays leagueAssignments by BUNK key into a DIVISION-keyed map — in-memory league changes never reach the scoped per-record cloud update**

- Evidence: Lines 4001-4007: `bunksToUpdate.forEach(bunk => { if (window.scheduleAssignments[bunk]) { assignments[bunk] = ...; } if (window.leagueAssignments?.[bunk]) { leagues[bunk] = window.leagueAssignments[bunk]; } });` — `window.leagueAssignments` is keyed by division name (scheduler_core_main.js:462 `window.leagueAssignments[block.divName]`), so `window.leagueAssignments[bunk]` is always undefined and `leagues` stays exactly `{ ...(scheduleData.leagueAssignments || {}) }` (line 3999) — the record's OL
- Scenario: A post-edit session updates league data in memory (e.g. a regenerated/edited league period) and the scoped saveModifiedBunksToCloud path is used to push the touched bunks. The cloud record's schedule rows update but its leagueAssignments are re-written unchanged with a NEWER updated_at — so on the next load, mergeSchedules' newest-wins per-division league merge (supabase_schedules.js:626-629) pref
- Verifier: Function name in the claim is wrong — there is no saveModifiedBunksToCloud; the code at unified_schedule_system.js:3859 is `bypassSaveAllBunks(modifiedBunks)` — but every quoted line and the mechanism check out exactly. 1) KEY MISMATCH IS REAL. At unified_schedule_system.js:3999 `leagues` starts as a spread of the cloud record's old division-keyed map, then lines 4001-4008 overlay `window.leagueA


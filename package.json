{
  "name": "daily-camp-schedular",
  "version": "0.0.0",
  "private": true,
  "description": "Browser-based camp scheduler (no build step).",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "test:e2e": "node tests/bunk_builder_ui.e2e.js"
  },
  "devDependencies": {
    "playwright": "^1.63.0"
  }
}
